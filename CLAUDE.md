@AGENTS.md

---

# LLM-WIKI 연동 규칙 (repo 내장 모드)

이 프로젝트의 **정본(설계 결정·ADR·측정 결과·과제·로그)은 repo 안의 `llm-wiki/`다.** 코드와 함께 버전 관리되고 함께 커밋된다.

- **세션 시작**: SessionStart 훅이 `llm-wiki/`의 최근 로그·열린 과제를 자동 주입한다. 상세가 필요하면 `llm-wiki/index.md`부터 진입한다(전체를 읽지 않는다).
- **세션 종료 전**: 의미 있는 작업을 했으면 `llm-wiki/log.md` 오늘 날짜 섹션(`## YYYY-MM-DD`)에 `- **[태그] 제목**: 내용` 형식으로 기록한다. **담당 태그는 `[FE]`(프론트엔드 코드) · `[BE]`(백엔드·인프라 코드) · `[PROJ]`(위키·문서·설정 등 코드 외) 중 하나**이며, FE와 BE를 모두 건드렸으면 태그를 두 개 붙이지 말고 항목을 쪼갠다. 코드 변경이 있는데 오늘 기록이 없으면 Stop 훅이 경고한다. 커밋은 코드와 함께 한다.
- **협업**: 프론트엔드 담당자가 따로 있다. 진입 문서는 `llm-wiki/Onboarding-FE.md`이며, 위 로그 태그·과제 형식·커밋 규칙이 거기 정리돼 있다. 규칙을 바꾸면 그 문서도 함께 고친다.
- **과제 관리**: 새 과제는 `llm-wiki/Next-Tasks.md`에 추가하고, 종료되면 종료 기록 표로 옮긴다. 상세 규칙은 아래 「Next-Tasks 작성 규칙」.
- **설계 결정**: ADR은 `llm-wiki/Decisions/NNNN-*.md`로 남긴다.

---

# Next-Tasks 작성 규칙 — 프론트엔드/백엔드 분리

`llm-wiki/Next-Tasks.md`의 열린 과제는 **프론트엔드와 백엔드를 별도 섹션으로 나눠서** 쓴다. 한 목록에 섞으면 "지금 브라우저만 열면 되는 일"과 "D1/Workers를 띄워야 하는 일"이 구분되지 않아, 세션 시작 시 주입된 목록만 보고는 착수 비용을 가늠할 수 없다.

## 파일 구조

```markdown
## 열린 과제 — 프론트엔드(FE)

### FE-1. 제목
**무엇** — …
**왜** — …
**완료 기준** — …

## 열린 과제 — 백엔드(BE)

### BE-1. 제목
**무엇** — …
**왜** — …
**완료 기준** — …

## 종료 기록
```

## 형식 계약 (훅이 파싱한다)

SessionStart 훅은 `### FE-` 또는 `### BE-`로 시작하는 줄만 추출해 주입한다(`.claude/settings.json`).

- **계약은 제목 접두사뿐이다.** `### FE-N. 제목` / `### BE-N. 제목` 형식을 지키면 된다. 섹션 제목은 파싱에 관여하지 않으므로 자유롭게 바꿔도 되고, 종료 기록으로 옮긴 과제는 접두사를 떼면 자동으로 주입에서 빠진다.
- 접두사가 없으면 세션에 주입되지 않는다 — 섹션 헤더는 버려지므로 접두사가 FE/BE를 구분하는 **유일한** 수단이다.
- 번호는 **FE·BE 각각 독립**으로 매긴다(FE-1, FE-2 / BE-1, BE-2). 과제 하나가 끝나도 남은 번호를 다시 매기지 않는다 — 로그·ADR의 참조가 깨진다.
- 추출 결과가 0건이면 빈 목록 대신 형식 오류를 알리는 문구가 주입된다. 세션 시작 시 그 문구가 보이면 제목 형식을 확인한다.

## 어디에 넣을지 판단하는 기준

| 과제의 성격 | 섹션 |
| --- | --- |
| `components/`·`app/`의 화면·상호작용·스타일, 클라이언트 전용 라이브러리(PDF.js, pdf-lib, culori) | FE |
| D1 스키마·마이그레이션, API 라우트, R2, Cron, rate limit, 배포·바인딩 | BE |
| 양쪽을 가로지름 | **쪼갠다** — 아래 규칙 |

가로지르는 과제는 **FE 과제와 BE 과제로 쪼개고, FE 쪽 `무엇`에 어떤 BE 과제에 의존하는지 명시**한다(예: `BE-3 완료 후 착수`). 쪼갤 수 없을 만큼 결합돼 있으면 BE에 두고, `완료 기준`에 FE 쪽 확인 항목을 함께 적는다.

의존 순서는 **BE 섹션 안에서만** 번호로 표현한다. FE 과제는 원칙적으로 서로 독립이어야 하며, FE끼리 순서 의존이 생기면 그건 과제를 잘못 쪼갠 신호다.

---

# 이 저장소의 검증 단계

공식 검증 입구는 아직 없다. 현재는 아래 두 명령이 전부이며, 우회 실행(예: `npx tsc` 단독, `next build` 직접 호출) 대신 npm 스크립트를 쓴다.

| 변경한 곳 | 1차로 돌릴 것 | 비고 |
| --- | --- | --- |
| 컴포넌트·lib 등 모든 TS/TSX | `npm run lint` | eslint(next 프리셋). 빠름 |
| 위와 동일 + 라우팅·설정(`next.config.ts`, `app/`) | `npm run build` | Turbopack 빌드 + TypeScript 검사 포함 |
| UI 동작(화면 전환·드래그·프리뷰) | `npm run dev` 후 브라우저 확인 | 자동 테스트 없음(수동). 테스트 도입은 열린 과제 |

실패 시 원인 분류 (실측 기준):

| 출력의 첫 토큰/패턴 | 분류 | 대응 |
| --- | --- | --- |
| `Type error:` (build 중) | 코드 문제 | 타입 수정 |
| `✖ N problems` (lint) | 코드 문제 | 규칙 위반 수정 |
| `sh: next: command not found` / `Cannot find module` | 환경 문제 | `npm install` — 코드로 고치려 들지 말 것 |
| `EADDRINUSE` (dev) | 환경 문제 | 기존 dev 서버(:3000) 종료 후 재시도 |
