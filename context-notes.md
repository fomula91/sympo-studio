# 인트로 페이지 재설계 — 컨텍스트 노트

## 요청 요약 (사용자 원문 요지)
유료 템플릿 추천 대신, 무료 섹션(Free Sections 필터로 확인)을 조합. 추천 조합은 Hero 01 +
Features 01 + Features 03(+ 선택적으로 Feature 04). 색상·서체·간격 디자인 토큰을 지정,
라이트/다크 둘 다 지원 요구. 최종 페이지는 7섹션 구성. 히어로 카피 별도 지정.

## 결정 1 — 디자인 토큰은 인트로 페이지에만 스코프
`lib/ui.ts`의 `UI.*`는 `--ink`/`--bg`/`--brand` 등 전역 CSS 변수를 참조하고, 콘솔·에디터·뷰어·
리포트 등 앱 전체 화면이 공유한다. 사용자가 준 새 색상(강조 `#087F70`/`#5EDBC5`)은 기존 `--brand`
(`oklch(0.475 0.11 205)`, 청록-블루 계열)와 색상 계열이 다르다(에메랄드-틸 vs 시안-블루) — 이걸
전역에 그대로 덮으면 앱 전체가 리브랜딩된다. 요청은 "인트로 디자인"이지 앱 전체 리브랜딩이 아니므로,
`--intro-*` 접두사로 별도 변수를 신설하고 `app/intro/page.tsx`에서만 참조한다. 다크 모드 전환은
기존 `ThemeToggle`이 `<html data-theme="dark">`를 토글하는 방식을 그대로 타므로, 새 변수도 같은
`:root` / `:root[data-theme='dark']` 셀렉터 아래 정의하면 토글 버튼이 그대로 작동한다.
**되돌릴 근거**: 사용자가 앱 전체 리브랜딩을 원하면 이 결정만 뒤집으면 된다(변수 스코프만 넓히면 됨).

## 결정 2 — Features 03(3개 모바일 화면), 최초엔 보류 → 2차 리뷰에서 실제 캡처로 해결
1차 라운드에서는 저장소에 자료 탭·Q&A 탭 상태의 스크린샷이 없고 신규 캡처가 브라우저 자동화의
기존 실패 이력(2026-09-14 FE-6 로그) 때문에 위험하다고 판단해, 기존 `viewer.png` 재사용 + 번호
텍스트 스텝으로 대체했었다. 사용자가 2차 리뷰에서 "실제 화면 3개를 각각 보여달라"고 재요청해
다시 시도했고, 이번에는 성공했다 — 상세 경위는 아래 결정 7 참조. 원래 우려(자동화 실패)는
스크린샷 재시도로 해결됐고, 진짜 막힌 지점은 전혀 다른 곳(스튜디오 프리뷰의 탭이 장식용이라 클릭이
안 먹힘, `.dev.vars` 미복사로 API가 500)이었다 — 예상한 리스크와 실제 리스크가 달랐던 사례.

## 결정 3 — 섹션 3의 "세 가지 전후 비교" 선정 기준
기존 `DESIGN_ROWS`(7건) 중 대표성 기준으로 3건 선정: ① 행사 식별(슬러그 재사용 문제 — 가장
직접적 피해 사례), ② 아젠다(이미지→구조화 — 반복 왕복 문제의 핵심), ③ 프리뷰(기존 `highlight:true`
로 이미 강조되어 있던 항목 — "프리뷰와 실물이 다르다"는 이 프로젝트의 핵심 주장과 직결).
나머지 4건(테마·접근성·참여·뷰어)은 이번 3섹션에서는 뺀다 — 6번 섹션(구현 범위)에서 압축 언급.

## 결정 4 — 히어로 큰 화면은 콘솔이 아니라 에디터 스크린샷
Hero 01 원형은 "큰 제품 화면"이 제품의 핵심 가치를 보여주는 자리다. 이 프로젝트의 핵심은
"운영자가 직접 편집 → 참가자 화면에 즉시 반영"이므로 정적 목록 화면(`console.png`)보다
편집+라이브 프리뷰가 함께 보이는 `editor.png`가 그 주장을 더 잘 보여준다. 기존 코드가 히어로에
`console.png`를 쓰던 것에서 변경.

## 미해결/보류
- 브랜드 프리셋 스와치(섹션 5)는 실제 `lib`의 프리셋 데이터를 못 찾아(export 미확인) 하드코딩
  값으로 대체 — 실제 프리셋 로직과 무관한 장식용 예시임을 명시.
- 모바일 폭 실측은 브라우저 자동화의 `resize_window`가 실제 CDP 스크린샷 해상도에 반영되지
  않아(1568×710 고정, `window.innerWidth`도 물리 화면 폭 그대로) 처음엔 완결하지 못했다 — 결정
  2에서 인용한 것과 같은 이 저장소의 기존 실패 이력과 일치한다. **우회법**: 현재 탭에 `<iframe>`을
  만들어 `style.width`를 직접 지정하면 iframe은 독립된 뷰포트를 가지므로 미디어쿼리·
  `matchMedia`·레이아웃이 그 폭 기준으로 정확히 반영된다(브라우저 창 자체를 리사이즈할 필요가
  없다). `javascript_tool`로 iframe을 만들고 `document.documentElement.scrollWidth`를
  `contentWindow.innerWidth`와 비교해 가로 오버플로우를 코드로 직접 검증했다.
  **이 방법으로 실제 버그 하나를 찾았다**: "현장의 문제" 섹션의 하이라이트 카드가
  `gridColumn: 'span 2'`를 폭 조건 없이 인라인 스타일로 걸어둬서, 그리드가 auto-fit으로 1열까지
  줄어드는 좁은 화면에서 암시적 2번째 열이 생겨 가로 스크롤이 발생했다(390px 뷰포트에서
  scrollWidth 417px 실측). `.compare-highlight` 클래스 + `@media (min-width:700px)`로 옮겨
  해결하고, 360~1920px 사이 9개 폭에서 재검증(전부 overflow 없음, `.feature-showcase`·
  `.compare-highlight` 브레이크포인트도 정확히 700px에서 전환됨을 `getComputedStyle`로 확인).
  이후 `/code-review`가 이 700px 자체가 그리드의 실제 2열 전환 지점(704px)보다 4px 낮다는 걸
  다시 찾아내 704px로 정정했다 — 근사값으로 정한 브레이크포인트는 그리드 계산식이 바뀌면 다시
  어긋날 수 있으니, globals.css 주석에 계산식(320*2+16=656, +패딩48=704)을 남겨뒀다.

## 결정 5 — 모바일 헤더 CTA 줄바꿈 버그 (FE 리뷰)
헤더가 `padding:'0 32px'`·`gap:32`를 고정값으로 쓰고 있었고, `ctaBtn`에는 `flex-shrink`·
`white-space` 지정이 없었다. 767px 아래에서 내비(`IntroNav`)가 사라져도 로고(~131px)+
토글(38px, 이미 `flex:'0 0 38px'`로 축소 방지)+CTA만 남는데, 이 셋+패딩+3개 gap의 합이 390px
뷰포트보다 커서 CTA가 찌그러졌다. 로고·토글은 이미 축소 불가(로고는 "SYMPO"가 공백 없는 한
단어라 줄바꿈 자체가 안 되고, 토글은 고정 flex-basis) 상태라 CTA만 찌그러지는 유일한 항목이었고,
그 결과가 "데모"/"열기" 사이 공백에서 줄바꿈 → 한글 음절 단위로 계속 줄바꿈(브라우저의 기본
한글 줄바꿈 규칙은 음절 단위 허용)까지 이어진 것이었다. `.intro-header-cta`에
`flex-shrink:0`+`white-space:nowrap`을 걸어 줄바꿈 자체를 막고, 480px 이하에서 패딩·gap을
줄여(32→16, 32→12) 전체 폭이 실제로 맞도록 했다(390px 실측으로 CTA가 108×54, 단일 줄임을
`getBoundingClientRect()`로 확인).

## 결정 6 — 제품 둘러보기 태블릿 레이아웃 (디자인 리뷰)
`FeatureShowcase`의 데스크톱 그리드(`minmax(220px,320px) 1fr`)를 700px 아래에서만 1열로
접었는데, 디자인 리뷰가 실측한 지점은 768px(태블릿)이었다 — 그 폭에서는 그리드가 여전히 2열
(320px+1fr)이라 화면이 358px로 쪼그라들어 있었다. 700px 컷오프 자체가 "태블릿 폭 문제"를
전혀 다루지 않고 있었던 것. 컷오프를 1024px로 올리고, 좁은 화면에서는 목록을 세로 나열 대신
가로 스크롤 탭(`.feature-tabs`에 `flex-direction:row`+`overflow-x:auto`)으로 바꿔 화면에
전체 폭을 내줬다. 탭이 가로로 눕는 상태에서는 설명 텍스트(`desc`)가 매 탭마다 길게 늘어지면
탭 목록 자체가 지저분해지므로 `.feature-tab-desc`를 1024px 아래에서 숨기고 라벨만 남겼다.

## 결정 7 — 참가자 경험 실제 화면 3장 캡처 (긴 우회 경로)
결정 2에서 보류했던 것을 2차 리뷰 요청으로 재시도했다. 겪은 문제와 해결 순서:
1. **스튜디오 에디터 Live Preview의 탭바(아젠다/자료/Q&A/설문)는 장식용이었다** —
   `components/Microsite.tsx:270-283`을 직접 읽어보니 `i===0`(아젠다)이 하드코딩된 활성 상태로
   고정돼 있고 `onClick`이 아예 없다. 클릭이 며 번을 시도해도 반응이 없던 게 자동화 문제가 아니라
   애초에 클릭 가능한 대상이 아니었던 것 — 반나절 삽질 끝에 소스를 읽고서야 알았다(교훈:
   DOM에서 안 되면 스크린샷/좌표를 더 정밀하게 잴 게 아니라 먼저 소스에서 핸들러 존재 여부를
   확인했어야 한다).
2. 대신 실제 상호작용(문서 카드 클릭 → `openDocument()` → `PdfViewer` 오버레이)은 `preview`
   플래그에서도 그대로 동작하도록 이미 짜여 있어서(`Microsite.tsx:70` `if (preview || !slug)
   setOpenDoc(doc)`), 이 경로로 PDF 뷰어 캡처는 성공했다.
3. **Q&A 참여 화면은 프리뷰에서 의도적으로 막혀 있다** — `Microsite.tsx:493` 부근에
   "질문 남기기 (미리보기 — 참가자 페이지에서만 동작)"라는 비활성 placeholder가 있다. 실제
   참가자 페이지(`/[slug]`)로 가야 했는데, `next dev`에서는 `GET /api/public/[slug]`가 500을
   내며 막혔다.
4. **500의 원인은 `DOC_URL_SECRET` 미설정** — `wrangler d1`/`next dev`는 Cloudflare 바인딩을
   부분적으로만 흉내 내고, 완전한 재현에는 `npm run preview`(`opennextjs-cloudflare build` +
   `wrangler dev`의 워커 프리뷰)가 필요했다. 이 worktree는 `.dev.vars`(로컬 시크릿, gitignore
   대상이라 worktree에 자동으로 안 옴)가 없어 `DOC_URL_SECRET`이 비어 500이 났다 — `llm-wiki`에
   이미 "DOC_URL_SECRET 미설정 시 500"이 기지의 지뢰로 적혀 있던 것과 정확히 일치. 원본
   체크아웃의 `.dev.vars`를 worktree로 복사해 해결(캡처가 끝난 뒤 다시 지웠다 — 로컬 전용
   시크릿을 worktree에 남겨둘 이유가 없다).
5. 프리뷰 워커의 로컬 D1(`worktree/.wrangler/state`)도 원본 체크아웃과 별개라 비어 있었다 —
   `npm run db:migrate`로 스키마만 만들고, 시드 데이터는 `curl
   http://127.0.0.1:8787/cdn-cgi/handler/scheduled`로 `worker/index.ts`의 `scheduled` 핸들러
   (자정 리셋 크론)를 수동 트리거해 채웠다(마이그레이션만으로는 시드 행이 안 들어간다 —
   `resetDemoData`는 크론 핸들러 안에서만 호출된다).
6. `resize_window`가 여전히 실제 뷰포트에 반영되지 않아 iframe 안에서 모바일 프레임을 다시
   재현했는데, 이번엔 **같은 origin의 실제 페이지를 iframe에 올려 그 안에서 진짜 클릭 이벤트를
   실행**하는 방식을 썼다(스튜디오 프리뷰 때처럼 좌표 추정에 의존하지 않고, `iframe.contentDocument`
   에서 텍스트로 대상을 찾아 `.click()`을 직접 호출) — 이 기법이 앞으로도 재사용 가능.
7. 캡처한 3장은 각각 1568×709 원본에서 `sharp`로 3:4 비율로 잘랐다(스튜디오 폰 프레임 영역
   405×564, Q&A 폼 영역은 270×360) — CSS `object-fit:cover`만으로 자르려던 최초 시도는 소스가
   와이드(1568×709)라 컨테이너의 세로 방향이 전혀 크롭되지 않아(계산상 scale이 항상 높이 기준으로
   맞춰짐) 원하는 영역이 카드 안에 다 안 들어왔다 — 사전 크롭이 CSS 트릭보다 확실했다.

## 결정 8 — 구현 범위 3열 레이아웃 (FE 리뷰)
`ScopeColumn`이 항목마다 라벨(고정 160px)과 설명을 좌우로 배치했는데, 이건 원래 페이지 폭
기준 3열 그리드 전체를 위해 설계된 게 아니라 1열 목록(`LimitList`, 지금은 제거됨)에서 그대로
가져온 레이아웃이었다. 1024px에서 3열로 쪼개지면 각 열 폭이 ~280-350px인데 라벨 160px을
빼면 설명에 120px 안팎만 남아 문장이 심하게 끊겼다. 기존 640px 미디어쿼리는 "페이지 전체 폭"만
보므로 이 문제(열 폭이 좁은 것이지 페이지가 좁은 게 아님)를 해결하지 못했다 — FE 리뷰가 정확히
이 지점을 짚었다. 좌우 배치를 버리고 항상 제목-위/설명-아래로 쌓게 바꿨고, 그 결과 이제 안
쓰는 `.intro-limit-row`(및 640px 미디어쿼리)를 globals.css에서 제거했다.

## 결정 9 — 본문 톤 조정 (사용자 요청: "말투가 딱딱하다")
히어로 제목·부제(`심포지엄 제작과 운영을...`, `행사 정보와 아젠다...`)는 사용자가 앞서 직접
문구를 지정했으므로 그대로 뒀다. 그 아래 섹션 소개문·카드 설명·주의 문구들은 전부 "-다" 종결의
건조한 서술체("왕복이 사라진다", "설계에 없다", "구조적으로 없앤다")였는데, 히어로의 "-습니다"
톤과 부딪히면서 전체적으로 딱딱하게 읽혔다. 본문·캡션을 "-어요/-해요" 위주로 통일하고, 일부는
어휘 자체도 더 구어적으로 바꿨다("왕복이 사라진다" → "번거로운 과정이 사라져요"). 섹션 제목·
배지·전후 비교의 before/after 라벨처럼 원래 짧은 명사구인 것들은 건드리지 않았다 — 그건
"딱딱함"이 아니라 정상적인 라벨 형식이다.
