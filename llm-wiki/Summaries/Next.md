# 다음에 무엇을 할까

> **사람이 읽고 바로 착수하는 문서.** 읽는 데 1분, 읽고 나면 무엇을 집을지 정해진다.
> 과제의 정본(무엇/왜/완료 기준 전문)은 [[Next-Tasks]]. 지금 상태는 [[Summaries/Status]].
> 기준일 2026-09-21.

## 용어

- **과제** — 앞으로 할 일 전체의 총칭. 전부 [[Next-Tasks]]에 있다.
- **Next-Task** — 과제 중 **먼저 해야 할 것**. 핫픽스 · 차단 · 필수 셋 중 하나에 해당한다.

---

## 지금의 Next-Task 1건

**FE-37 하나다.** FE-28은 코드 변경 없이 **기각(판단으로 종료)** 됐다 — PR #48의 전제("hits가 부풀어 순유효 방문자 수를 왜곡한다")가 코드 확인 결과 틀렸다(운영자 리포트는 `hits`를 아예 안 읽고, `visitors`는 이미 SQL에서 중복 제거되며, `hits`는 애초에 "몇 번"을 재는 별개 지표로 설계됨). 상세는 [[Archive/Closed-Tasks]]. 그리고 **BE는 열린 과제가 0건이다** — 2026-09-14~17에 BE-13·15·24·25·26·
27·28·29·30·31을 닫았고, BE-22는 커스텀 도메인까지만 하고 엣지 rate limiting을 기각
([[0013-edge-rate-limiting-rejected]]), BE-32(제공자 연결 흐름)는 **모델 결정으로 기각**했다
(제공자별 별개 계정이 채택된 모델이다 — [[0011-account-link-key]] 「개정」).

**즉 지금 남은 일은 전부 프론트엔드다.** 새 BE 작업은 과제를 발굴하거나, 실제 사용에서
문제가 드러날 때 생긴다.

> **FE-30(스튜디오↔D1 연결 경로)은 종료됐다** — id 지목 경로로 결정·구현([[0010-studio-d1-slug-designation]]).
> FE-15·19·23·24·25는 더 이상 막혀 있지 않다. 단, 이 연결이 다룬 건 기본 정보 필드뿐이고
> **아젠다·자료 편집을 서버에 반영하는 건 여전히 FE-24·FE-25의 몫**이다.

### 🔴 1. FE-37 — 브랜드 이름 자리에 색 프리셋 라벨이 나온다 `필수`

참가자 화면 상단의 브랜드 이름이 **행사 브랜드가 아니라 색 프리셋의 이름**이다. 운영자가 이벤트를 만들 때 브랜드명(`MERIDIAN` 같은)을 반드시 입력하는데, 그 값은 운영자 콘솔 검색에만 쓰이고 참가자는 볼 일이 없다.

여태 눈에 안 띈 이유는 추출 프리셋이 항상 기본 색으로 폴백돼서 "슬레이트 뉴트럴"이라는 무난한 글자가 나왔기 때문이다. **FE-31이 그 폴백을 걷어내면서 진짜 프리셋 이름이 그대로 나오기 시작한다** — 그리고 그 이름의 기본값은 운영자가 올린 **이미지 파일명**이다. 실제로 확인해 보니 화면 상단에 `LOGO-FINAL-V3`가 찍혔다.

- **첫 걸음** — `app/[slug]/page.tsx`의 `brandLabel: preset.label`을 `data.brand`로 바꾼다. 값은 서버가 이미 내려주고 있어서 새로 배선할 게 없다.
- **같이 봐야 할 곳** — 스튜디오 미리보기(`ViewerScreen`·`EditorScreen`)도 같은 코드라 함께 고쳐야 미리보기와 실제 화면이 맞는다.
- **백엔드 할 일은 없다.**

---

## Next-Task 말고, 지금부터는 흐름 묶음 순서로 (2026-09-21 재편)

**"서로 독립이니 아무거나 집으면 된다"는 더 이상 맞지 않다.** 브라우저로 직접 확인해보니 —
새 이벤트를 만들어도 새로고침하면 사라지고, 새 이벤트인데 자료가 4건(전부 목업)으로 뜨고,
"공개하기"를 눌러도 미리보기만 열리고, 리포트는 방금 만든 행사가 아니라 샘플 수치를 보여준다.
**컴포넌트 하나씩은 끝났다는 완료가 쌓였는데, "행사를 만들어 참가자가 확인한다"는 흐름
자체가 어디서도 안 이어져 있었다.** 그래서 지금부터는 이 순서로 묶어 착수한다 — 번호는
그대로 두고, 화면에서 끝까지 검증되는 단위로만 묶는다. 상세 범위·근거·완료 기준은
[[Next-Tasks]]의 "전체 조망" 표를 본다.

| 순서 | 묶음 | 지금 상태 |
|---|---|---|
| 0 | 이미 작업한 브랜치 통합 | **끝났다** — FE-13·27·29·31·32·34·35 전부 머지, FE-28은 기각 종료 |
| 1 | 내 행사를 만들고 다시 열기 (FE-15 축소판) | **FE-15 완료**(게스트 실측, 로그인 경로 미실측) — 남은 건 FE-36·38(작은 독립 수정) |
| 2 | 아젠다를 편집하고 실제 공개하기 (FE-23+24) | 미착수 — **다음에 집을 것** |
| 3 | 자료를 올리고 참가자가 읽기 (FE-25) | 미착수 |
| 4 | 브랜드 편집 결과를 그대로 전달하기 (FE-19·31·18·37·40) | FE-31만 완료 |
| 5 | 참가자 동선 완성하기 (FE-17) | 미착수 |
| 6 | 계정 기능 확장·구조 정리 (FE-39·14) | 미착수 |

**FE-15가 끝났다** — 로그인·게스트 구분 + 콘솔이 실제 D1 이벤트에 연결됐다(게스트 경로는
브라우저로 하드 리프레시까지 확인, 로그인 경로는 이 환경에 실제 Google 계정이 없어 코드·
빌드로만 확인). **다음은 묶음 2(FE-23+24)다** — 아젠다 저장(`PUT .../sessions`)이 이미
서버에 구현돼 있는 것도 확인했으니 순수 FE 작업이다.

**FE-14(FSD 재편)는 뒤로 미룬다** — 데이터 소유권·저장 흐름이 묶음 1~4에서 아직 바뀌는
중이라, 지금 옮기면 다시 옮기게 된다.

---

## 착수하기 전에

1. **[[Next-Tasks]]에서 그 과제의 `왜`를 읽는다.** 이 프로젝트는 무엇을 만드는지보다 왜 그렇게 만드는지가 결과물이다.
2. **브랜치를 판다.** `main` 직접 커밋은 하지 않는다.
3. **처음이라면 [[Onboarding-FE]]를 먼저** — 커밋 규칙, 로그 기록 형식, 로컬 환경 준비(`DOC_URL_SECRET`·로컬 D1)가 거기 있다.

## 끝나고 나면

1. **[[log]]에 오늘 날짜로 기록** — `- **[FE] 제목**: 내용`. 코드와 같은 커밋에.
2. **[[Next-Tasks]]에서 그 과제를 [[Archive/Closed-Tasks]]로 옮긴다.** 번호는 다시 매기지 않는다.
3. **새로 발견한 것이 있으면 과제로 등록한다.** 핫픽스·차단·필수면 Next-Task로, 아니면 FE/BE 백로그로.
