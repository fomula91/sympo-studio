# sympo-studio — Log

이 프로젝트의 시간순 기억. 최신이 위로.

> **형식 계약 (훅이 파싱한다)**: 날짜 섹션은 `## YYYY-MM-DD`, 항목은 `- **[태그] 제목**: 내용`.
> 코드 repo의 SessionStart 훅이 최신 섹션의 **제목**들만 추출해 세션에 주입한다(태그 포함).
> 제목은 주입만 보고도 무슨 일이 있었는지 알 수 있게 쓴다.
>
> **담당 태그** — 여러 사람이 작업하므로 항목마다 하나씩 붙인다. [[Onboarding-FE]] 참조.
> `[FE]` 프론트엔드 코드 · `[BE]` 백엔드·인프라 코드 · `[PROJ]` 위키·문서·설정 등 코드 외 작업.
> 한 항목이 FE와 BE를 모두 건드렸다면 **항목을 쪼갠다** — 태그를 두 개 붙이지 않는다.

> **과거 기록은 아카이브에 있다** — 이 파일은 최근 3개 날짜만 둔다(훅은 최신 섹션만 읽는다).
> [[Archive/log-2026-09]] · [[Archive/log-2026-08]] · 사람이 읽을 연표는 [[Summaries/History]]

## 2026-09-19
- **[FE] FE-28 — `doc_view` 로그 중복 방지. 마지막 남은 Next-Task를 닫아 열린 Next-Task가 0건이 됐다**: `Microsite.tsx`의 `openDocument`가 열 때마다 조건 없이 `doc_view`를 보내던 것을, `session_view`가 이미 쓰던 패턴(`seenSessionsRef`)을 그대로 맞춰 고쳤다 — `seenDocsRef`(Set)에 없는 문서만 보내고, 보낸 뒤 바로 추가한다. 전송이 실패하면(`ok === false`) `seenDocsRef`에서 다시 빼 재시도 가능하게 했다(`session_view`가 재조회 시 다시 관찰되도록 `seen`에서 빼는 것과 같은 이유, FE-21). **실측 방법이 이번 항목의 요점이다** — 로컬 시드에는 documents가 0건이라, `PUT .../documents`로 메타를 만들고 `PUT .../documents/[docId]/upload`에 실제 PDF(`public/demo/sample-lecture.pdf`)를 올려 `status: 'ready'`인 진짜 자료를 먼저 만들었다. 그 뒤 브라우저로 참가자 페이지(`/meridian-arte-seoul-260815`)에서 같은 자료를 두 번 열고 `GET /api/events/1/ops`로 확인하니 `documents[].hits`가 **1로 유지**(두 번째 열람에서 `/logs` 요청 자체가 안 나가는 것을 network 탭으로 확인). `npm run lint`는 변경 파일(`Microsite.tsx`) 기준 오류 0건 — 저장소 전역 lint는 `.claude/worktrees/fe-intro-redesign/`의 gitignore된 임시 `worker-configuration.d.ts`가 `eslint.config.mjs`의 무시 패턴에 안 걸려 무관한 오류 수천 건을 같이 뱉는다(기존부터 있던 환경 노이즈, 이번 변경과 무관해 손대지 않았다). 열린 과제가 **12건**(Next-Task 0 · FE 12 · BE 0)이 됐다.

## 2026-09-17
- **[PROJ] README를 168줄 → 96줄(62% 감소)로 압축하고, 그 과정에서 낡은 서술 8곳을 잡았다**: 사용자가 "핵심만 남기고 전부 삭제"를 요청했다. **압축 기준은 "이 문서가 포트폴리오의 본체"라는 원래 목적**이었다 — 이 저장소를 남과 구별짓는 것은 CRUD 구현이 아니라 **현장에서 겪은 문제 관찰**이므로, 「문제」 절의 두 층 구조와 "관찰/가설 분리"는 압축하되 **뼈대를 남겼고**, 나머지를 잘랐다. 없앤 것: 「관찰과 가설을 갈라 둡니다」 절(한 문단으로 흡수) · 「다시 만들면서 바꾼 것」(설계 판단과 중복) · 「화면」 표 2개(상태 표 1개로) · 「데이터에 대하여」(상단 고지로 흡수) · 설계 판단 표 7행 → 5행 · 기술 선택 4문단 → 불릿 4개. **압축보다 중요했던 건 사실 정정이다** — 읽어보니 README가 현재와 8곳에서 어긋나 있었다: ① "API 18개"(실제 20) ② "테스트 23건"(실제 99) ③ "운영자 스튜디오가 서버에 연결된 적이 없다 — 서버 호출이 한 줄도 없다"(FE-30이 기본 정보를 붙였다. 코드로 확인: `StudioProvider`에 fetch 3건) ④ "목록·상세·운영 지표 조회와 프리셋 쓰기는 인가 검사가 없다"(**BE-13·24·25가 전부 닫았다** — `/ops`·`/events`·`/presets` 라우트에서 직접 확인) ⑤ "강의자료 뷰어에 포커스 트랩이 없다"(FE-26이 닫았다) ⑥ "맞는 층은 엣지이며 커스텀 도메인이 필요하다"(도메인이 생겼고 **기각했다** — ADR 0013 링크로 대체) ⑦ 화면 표의 ⚠️ 목업 4개(에디터는 이제 부분 연결) ⑧ 배포 주소. **"의도적으로 안 하는 것"이라는 항목을 새로 만들었다** — 엣지 rate limiting 기각처럼 *못 한 것이 아니라 안 하기로 한 것*은 한계와 성격이 달라서, 섞어 두면 읽는 사람이 미완으로 오해한다. 링크 5개 전부 유효 확인.
- **[PROJ] 위키를 코드와 대조해 표류 6곳을 잡았다 — 그중 하나는 FE가 못 보고 있던 엔드포인트다**: 머지할 것은 없었고(main, 변경 0, 미머지 0) 위키 정합성만 봤다. **가장 실질적인 발견**: `app/api`의 라우트 20개를 [[API-Guide-FE]]와 하나씩 대조하니 **`GET /api/public/[slug]/report`가 문서에 없었다.** BE-24가 `/ops`를 소유자 전용으로 잠그면서 갈라 낸 참가자용 공개 집계 경로인데, 문서에 없으면 **FE는 계속 운영자 경로(`/ops`)를 불러 404를 맞는다.** 요약표와 상세 절을 더하고 *"참가자 리포트는 이걸 쓴다, `/ops`는 소유자 전용이라 404"*를 명시했다 — 인증이 필요 없는 이유(담기는 건 집계 수치뿐이고 애초에 공개된 행사의 것)와 경계가 소유권이 아니라 **공개 상태**라는 것도 함께. 이제 **20/20 문서화**됐다. **나머지 표류 5곳**: ① 테스트 수 — 어제 `rate-policy.test.ts`를 추가하면서 "11파일 83건"이 "12파일 99건"으로 어긋났다(Context 2곳·Status 1곳). **내가 만든 표류를 내가 못 잡은 것**이라, 다음부터 테스트 파일을 늘리면 그 수치가 박힌 곳을 함께 본다. ② [[Onboarding-FE]]·[[Summaries/Next]]가 **종료·기각된 BE 4건(BE-22·27·28·32)을 여전히 "할 일"로** 들고 있었다 — 쉬운 것/고민 필요한 것 표에서 빼고 FE로 채웠으며, "BE 열린 과제 0건"을 명시했다. ③ `Next-Tasks`의 「전체 조망」 날짜가 09-15에서 멈춰 있었다. ④ 같은 파일의 ⚠️ 절이 **이제 전부 종료된 BE-24~29를 살아있는 경고처럼** 적고 있었다 — 교훈만 남기고 상태를 갱신하면서, 이번에 겪은 **두 번째 사고(완결된 구현이 미머지 브랜치에 묵어 목록과 어긋남, 마이그레이션 0007·0011 결번도 같은 원인)**를 나란히 적었다. 두 교훈이 같은 자리에 있어야 다음에 둘 다 읽힌다.
- **[PROJ] BE-32 기각 — 제공자별 별개 계정을 채택된 모델로 확정했다. BE 열린 과제가 0건이 됐다**: 사용자 판단이 출발점이었다 — *"이메일이 달라지거나 다른 계정으로 로그인해야 되는 상황도 있어서 굳이 하나의 계정으로 안 묶어도 될 것 같다."* 검토해보니 근거가 더 있었다: ① 제공자가 Google 하나뿐이라 **갈릴 일 자체가 아직 없다**(존재하지 않는 문제였다) ② **연결을 안 하면 탈취 표면이 0이다** — 연결 기능은 "이 `sub`를 이 계정에 붙여도 되는가"라는 판정을 하나 더 만드는데 그게 틀리면 곧 계정 탈취이고, **그게 BE-26이 닫은 바로 그 구멍**이다 ③ "이메일이 바뀌는 상황"은 BE-26·BE-30이 **이미 처리했다**(이메일은 표시용, 겹쳐도 별개 계정) ④ 강제로 묶는 건 제품이 "이 둘은 같은 사람"을 단정하는 것이라 업무용/개인용 분리를 방해한다. **과제를 지우는 것으로 끝내지 않은 이유**가 중요하다 — [[0011-account-link-key]]의 「감수하는 것」이 *"옳은 해법은 명시적 연결 흐름"*이라 적고 「재검토 트리거」가 BE-32를 가리키고 있어서, 그냥 지우면 **트리거가 죽은 링크로 남고 나중에 누가 "같은 사람이 갈리네"에서 가장 쉬운 해법인 이메일 매칭을 되살릴** 수 있다(그 유혹이 정확히 그 문서가 막으려고 쓰인 것이다). 그래서 두 항목을 **개정**하고 「개정」 절을 새로 달아 **갈리는 것은 결함이 아니라 의도**임을 박았다. 트리거도 바꿔 적었다 — 두 번째 제공자를 붙일 때 할 일은 연결 UI가 아니라 `upsertUser`가 `provider`를 하드코딩된 `'google'` 대신 인자로 받게 하는 것뿐이다. **감수하는 것**: Google 계정을 잃으면 그 계정의 이벤트에 못 들어간다 — 다만 이건 연결이 아니라 **「계정 복구」라는 다른 문제**이고(둘을 이어놔도 둘 다 잃으면 같다) 운영자 데이터가 실제로 쌓이는 단계에 오면 과제로 세운다. **BE 섹션이 비었지만 헤더는 남겼다** — 훅이 `## 열린 과제` 섹션에서 `### BE-`를 추출하므로 헤더를 지우면 다음 BE 과제가 주입에서 통째로 빠진다. 열린 과제 13건(Next-Task 1 · FE 12 · **BE 0**), 종료 50건.
- **[PROJ] 정본 주소를 `sympo.superjacob.com`으로 옮기고, 그 김에 낡은 연표를 이어 썼다**: 옛 `.workers.dev` 주소가 9곳에 있었는데 **전부 바꾸지 않았다.** 바꾼 것은 **현재 상태·운영 안내 4곳**(README·[[API-Guide-FE]]·[[Context]]·[[Summaries/Status]])이고, **유지한 것은 그때의 사실**이다 — `Archive/` 2곳(규칙상 원문 그대로), [[0008-rate-limit-counter]]의 *"지금 배포는 workers.dev이고 거기엔 WAF를 못 건다"*(**기각 근거 문장이라 바꾸면 0013의 부분 개정이 안 읽힌다**), [[Summaries/History]]의 BE-2 항목. API-Guide와 Context에는 **구 주소도 살아 있다**는 것을 명시했다 — 밖으로 나간 링크를 죽이지 않으려고 `workers_dev: true`로 둘 다 유지 중이다. **그 김에 [[Summaries/History]]가 9/14에서 멈춰 있는 것을 발견했다.** ⑩(9/15~9/17)을 추가하고 — 스튜디오↔D1 연결·인가 경계·계정 연결 키·업로드 예약·커스텀 도메인·Q&A 한도 — **「되돌아보면」의 낡은 결론을 고쳤다**: *"아직 안 메운 가장 큰 구멍은 스튜디오↔D1 연결"*이 더는 사실이 아니다(FE-30이 닫았다). 대신 이번에 실제로 드러난 패턴을 적었다 — **"이미 다 해놓고 과제 목록엔 열려 있는" 상태의 반복**(BE-15·BE-27이 완결된 브랜치로 묵었고, 마이그레이션 0007·0011 결번도 같은 원인)과 그래서 생긴 규칙(착수 전 `git branch --no-merged main`).
- **[BE] Q&A 창 한도 3→10, IP 창 20→60 — 하루 한도는 건드리지 않았다**: 사용자의 "네트워크 보안이 과한 것 같다"는 판단을 받아 Q&A 정책만 완화했다. 요점은 **노출의 천장은 하루 한도이고 마찰은 창 한도에서 온다**는 분리다 — 창을 올려도 **하루에 쓸 수 있는 총량은 1도 늘지 않는다**(브라우저 30·IP 300 그대로). 3이 문제였던 이유는 남용이 아니라 **데모 마찰**이다: 눌러보는 사람이 질문을 연달아 던지면 4번째에서 429를 맞고 "고장났다"로 읽는다. 0006이 재검토 트리거에 적어둔 "정상 사용에서 429가 관측되면 상한 재조정"에 해당한다. **IP 창을 함께 올린 이유**가 중요하다 — 브라우저 창만 10으로 올리면 열심히 눌러보는 **한 사람이 행사장 창 한도(20)의 절반을 혼자 먹는다.** 0006이 IP 창 20을 "120명 행사 피크를 막지 않는 선"으로 잡았으므로 두 값은 함께 움직여야 한다. **실측**: 같은 토큰 연속 전송 → 10건 통과·11번째부터 429(문구가 상수를 보간해 "60초에 10건"으로 자동 갱신), 하루 카운터를 29로 맞추고 창을 비운 뒤 → 30번째 통과·31번째 "오늘 남길 수 있는 질문 수(30건)를 모두 사용했습니다" — **하루 천장 불변 확인.** **다른 정책은 안 건드렸다** — survey(창 60)·logs(창 60)·events(창 25)·upload(5분 200MB)는 이미 넉넉하고 근거도 적혀 있어, 빡빡한 건 Q&A 하나뿐이었다. 값이 아니라 **값들 사이의 관계**(창 ≤ 하루, IP ≥ 브라우저, 창 길이 > 0)를 `lib/rate-policy.test.ts` 16건으로 고정했다 — ADR 없이 바뀌는 자리라 관계가 깨지면 증상이 조용하다. `lint`·`test`(99건)·`build` 통과.
- **[BE] BE-22 종료 — 커스텀 도메인은 붙이고, 엣지 rate limiting은 기각했다**: 사용자가 `superjacob.com`을 보유 중이라 **선행 조건이 구매 없이 풀렸다** — 이미 Cloudflare 네임서버(keira/keanu)를 쓰고 있어 존이 존재했다. `sympo.superjacob.com`을 `routes` + `custom_domain: true`로 붙였고 `/intro`·`/console`·`/api/presets`·`/api/public/[slug]` 전부 200 실측. **사고 하나**: `routes`를 지정하면 wrangler가 **workers.dev 서브도메인을 기본으로 끈다**. 모르고 배포해 기존 주소가 전 경로 404(`error code: 1042`)가 됐고, 배포 후 실측에서 잡아 `workers_dev: true`로 되살렸다 — README·위키 9곳이 그 주소를 가리키고 있어 두 주소가 함께 살아 있어야 한다. 사유를 설정 파일 주석에 남겼다. **엣지 규칙은 기각했다**([[0013-edge-rate-limiting-rejected]]). 0002가 지시한 대로 공식 문서로 무료 플랜 제약을 재확인했더니 **Rate Limiting Rule의 카운팅 기준이 IP 단독**이었다 — [[0006-rate-limit-key]]가 "행사장은 단일 egress IP라 방 전체가 잠긴다"며 기각한 바로 그 방식이라, 걸면 **정상 행사를 깨뜨리는 경로를 엣지에서 되살린다.** 산술로도 양립하지 않는다: 참가자 100명 폴링이 한 IP 10초당 200~330 요청인데, 그걸 안 막는 임계값(10초당 1000)은 **초당 100 요청을 통과시켜 17분이면 Workers 일일 한도 10만을 태운다.** Managed Challenge는 IP 문제를 피하지만 0006이 CAPTCHA를 기각한 이유(고령 사용자 마찰, 이 제품이 푸는 문제가 정확히 그것이다)가 그대로 적용돼 참가자 경로에는 안 건다. **남긴 판단**: 0008의 "엣지가 맞는 층"은 유효하고 **무료 플랜에서는 실현되지 않는다**는 것을 확정했다(0008에 부분 개정 포인터). 얻은 것은 **존 확보** — 플랜을 올리면 바로 걸 수 있다. 착수 배경에 사용자의 "네트워크 보안을 좀 느슨하게 가도 된다, 지금 과한 것 같다"는 판단이 있었고, 실제로 문서를 확인해보니 그 방향이 맞았다 — 다만 **업로드 상한(R2에 결제 수단이 붙어 있다)과 인가 경계는 같은 층이 아니라 그대로 둔다.**
- **[PROJ] 브랜치 26개 → 1개로 정리 + 원격 마이그레이션 0015 적용, 그리고 [[Context]]의 낡은 수치 정정**: 로컬 25개·원격 10개를 지워 **로컬은 `main` 하나, 원격은 `main` + 폐기 예정 2개**만 남겼다. 지우기 전에 **원격 사본이 없는 로컬 13개가 전부 main에 머지됐는지 하나씩 확인**했고(커밋이 main 히스토리에서 도달 가능해야 잃는 것이 없다), 머지된 것은 `git branch -d`(미머지면 git이 거부한다)로, 대체된 2개만 origin 사본을 확인한 뒤 `-D`로 지웠다. 삭제 전 SHA를 파일로 남겼다. **남긴 2개**(`feat/be-12-auth`·`docs/pr9-cross-review`)는 미머지지만 **머지하면 main이 깨진다** — 전자는 이미 적용된 `0010_auth.sql`과 같은 테이블을 만드는 `0007_auth.sql`을 되살리고 인증 코드를 2026-09-07로 되돌리며, 후자는 `Next-Tasks.md`를 2026-08-31 파일 전체로 덮어 **종료된 FE-1~9·BE-2~16을 열린 과제로 부활**시킨다. 둘 다 내용은 이미 main에 있다(cherry-pick / 종료 기록). 원격 브랜치 삭제 전에 **열린 PR이 걸려 있는지 확인**했다 — #46은 huni2 포크의 브랜치라 무관했다. 원격 D1에 `0015`를 적용해 **로컬·원격이 0001~0015로 동기화**됐다(백필 대상 0건 — 원격 `documents`가 0건이다. 백업은 뜨고 진행, 적용 후 행 수 7종·운영 엔드포인트 5종 무변화 확인). **[[Context]]를 함께 고쳤다** — 테스트 "23건 / 3파일"이 실제로는 **83건 / 11파일**이었고 마이그레이션 서술도 `0010`에서 멈춰 있었다. 결번을 **0007 하나에서 0007·0011 둘로** 고치며 두 사고가 같은 원인(미머지 브랜치에 묵는 사이 뒤 번호가 먼저 적용됨)임을 적었다.
- **[PROJ] 미머지 로컬 브랜치를 훑어 BE-27·BE-28이 이미 구현돼 있던 것을 발견 — 그리고 BE-28을 중복 구현했다**: 사용자가 "작업 완료된 항목은 따로 로컬 브랜치가 있는 걸로 안다"고 짚어 `git branch --no-merged main`을 봤더니, **`be-27-document-status`와 `be-28-demo-theme-reset`에 완결된 구현이 있었다.** BE-28은 내가 그날 처음부터 다시 구현한 뒤였고, 두 구현이 `DEMO_THEME` 상수 접근·컬럼 목록·가짜 D1의 `demoExists: false` 확장까지 **독립적으로 같은 결론에 도달**해 있었다(코드 결과물은 동등). **착수 전에 브랜치를 확인하지 않은 것이 원인**이다 — BE-15도 같은 이유로 종료 기록과 열린 과제 양쪽에 있었고, 이 저장소에서 반복되는 패턴이다. 앞으로 과제 착수 전 `git branch --no-merged main`을 먼저 본다. 두 브랜치를 머지했다: BE-28 쪽은 main에 동등 구현이 이미 있어 **`eslint.config.mjs`의 `.wrangler/**` 무시 규칙과 더 엄격한 테스트 단언(`= ?` 바인딩까지 고정)만** 취했고, 위키는 브랜치가 BE-26 시점에서 갈라져 있어(BE-24·25·28·30을 열린 과제로 되살리려 했다) 전부 main 쪽을 택했다.
- **[BE] BE-27 마이그레이션 번호를 0011 → 0015로 옮겼다**: 이 마이그레이션이 미머지 브랜치에 묵는 사이 **0012~0014가 먼저 적용됐다**(원격 포함). 앞 번호를 두면 "적용된 0014 뒤에 0011이 적용되는" 이력이 남는다 — 내용이 앞 셋과 독립이라 순서 의존은 없다. **BE-12가 0007→0010으로 옮긴 것과 같은 판단**이고, 사유를 마이그레이션 파일 머리에 남겼다. 덧붙여, 앞서 내가 "설명 없는 결번"이라고 보고했던 `0011`의 정체가 **바로 이 브랜치**였다 — 결번이 아니라 묵고 있던 번호였다.
- **[BE] BE-27 — 자료 status를 검사하는 대신 `r2_key`에서 파생시켰다**: `PUT …/documents`가 본문의 `status`를 그대로 저장하는데 검사는 **신규 생성에만** 있었다(`id === null && status === 'ready'` → 400). 기존 항목 수정 경로에는 없어서 **`r2_key`가 NULL인 자료를 메타 저장 한 번으로 `ready`로 올릴 수 있었다** — 참가자 화면에 **열 수 없는 자료가 "준비됨"으로** 뜬다. FE-6이 `unavailable = pending || !url` 게이트로 막아둔 것은 증상을 가린 것이지 원인이 아니고, 서버가 일관성을 보장하지 않으면 다른 소비자(운영자 화면·리포트)가 같은 함정을 각자 다시 밟는다. **검사를 한 군데 더 넣는 대신 값의 출처를 없앴다** — `validateDocumentsBody`가 `status`를 아예 읽지 않고, 라우트가 `UPDATE … SET status = CASE WHEN r2_key IS NULL THEN 'pending' ELSE 'ready' END`으로 같은 문에서 파생시킨다(신규 INSERT는 항상 `'pending'`). **검사는 잊을 수 있지만 파생은 잊을 수 없다** — 예전 실패가 정확히 "한쪽 경로에만 검사를 넣었다"였다. **이미 어긋나 있는 행은 코드로 안 고쳐지므로** 마이그레이션 `0015_document_status_from_file.sql`을 함께 넣었다(파생과 같은 식으로 정정, 데이터는 안 지운다 — `pending`은 결함 표시가 아니라 "연자 자료가 아직 안 왔다"는 이 테이블의 정상 상태다). **실측**: 파일 없는 자료에 `ready`를 실어 PUT → 응답 `pending` / 업로드 후 `ready` 회귀 없음 / 그 뒤 `pending`을 실어 이름만 바꿔 저장 → **`ready` 유지 + 이름 반영** / 새 자료에 `ready` → 400이 아니라 `pending`으로 생성 / 마이그레이션은 어긋난 행을 일부러 만들어 적용해 정정 확인. 테스트 데이터는 정리했다. `lib/agenda.test.ts` 신설 4건, `npm run lint`·`test`(32건)·`build` 통과. **FE 계약 변화**: `status`를 보내도 무시된다 — API-Guide-FE에 "응답의 `status`를 그대로 쓰라"로 명시했다.

## 2026-09-16
- **[BE] BE-28 — 데모 리셋이 테마를 안 되돌리던 것. 고친 건 한 문장인데 원인은 두 경로의 비대칭이었다**: `resetDemoData`의 UPDATE에 테마 6컬럼(`preset_id`·`mode`·`icon_set`·`density`·`key_visual`·`kv_pattern`)이 빠져 있었다. **왜 이게 조용했는가**: 데모 행이 없을 때 타는 INSERT 분기는 그 컬럼들을 아예 안 써서 **스키마 DEFAULT가 채웠고**, 매일 자정의 정상 경로인 UPDATE만 빠져 있었다 — 즉 "새 DB에서는 맞고 운영 중에는 틀린" 상태라 새로 깔아 보는 검증으로는 안 잡힌다. 데모는 `owner_id IS NULL`이라 누구든 편집할 수 있어(게스트 체험이 여기 기댄다) 방문자가 바꾼 테마가 다음 방문자에게 그대로 남았다. **UPDATE에 6컬럼을 더하는 데 그치지 않고 `DEMO_THEME` 상수를 두어 INSERT·UPDATE가 같은 값을 보게 했다** — 뿌리가 "한쪽은 DEFAULT에 기대고 한쪽은 명시해야 한다"는 보이지 않는 비대칭이었으므로, 그걸 없애지 않으면 컬럼이 늘 때마다 같은 사고가 난다. **범위를 전수로 검증했다** — `events` 23컬럼을 SET 목록과 대조해 안 되돌아오는 9개를 추렸고, `slug`(데모 행을 **찾는** 키)·`created_at`(행 생성 시각)은 되돌릴 대상이 아니며, **이번 머지로 막 들어온 `client_ref`(0013)는 쓰기 경로가 `POST /api/events/import`의 신규 생성뿐이고 그마저 `owner_id = 나`와 함께 도는데 데모는 `owner_id IS NULL`이라 닿지 않는다**고 확인했다(새 컬럼이라 의심했는데 과제 범위가 정확했다). `key_visual`을 NULL로 밀어도 R2 고아는 안 생긴다 — 키비주얼이 R2로 가는 경로가 아직 없다(FE-19가 `blob:` 문제로 열려 있다). **검증**: 회귀 테스트 2건을 넣고 **수정을 되돌려 실제로 실패하는지 확인했다**(안 그러면 통과하는 테스트가 아무것도 고정하지 않는다). 가짜 D1에 `demoExists: false`를 더해 **INSERT 분기도 검사 대상에 넣었다** — 기존 하네스는 조회가 항상 행을 돌려줘 그 분기가 영영 안 돌았다. 값 복귀는 가짜 D1이 못 보므로(SQL 문자열만 본다) **코드가 발행하는 SQL과 바인딩을 그대로 뽑아 로컬 D1에 걸었다**: 테마를 `amber/dark/solid/컴팩트/외부URL/grid`로 오염시킨 뒤 리셋하니 6컬럼이 전부 초기값으로 복귀했고, 바인딩 순서도 이 경로로 확인했다. `lint`·`test`(79건)·`build` 통과.
- **[PROJ] 열린 PR 3건 검토 후 #47·#44를 main에 머지 (#46은 판단 보류로 열어둠)**: 사용자가 "pr 올라온거 확인해줘"로 시작해 셋을 훑고 #46(FE-30 revert)을 먼저 리뷰했다. **되돌리기 자체는 완전했다** — 트리가 `4af261e^1`과 바이트 단위로 같고, dangling import도 ADR 링크 잔여도 없고, 훅 파싱(`## 열린 과제`)도 정상이었다. 다만 **본문의 "준비되면 다시 머지하면 됩니다"가 사실과 달랐다**: 머지 커밋을 revert하면 git은 그 브랜치를 여전히 머지된 것으로 기록해, `merge-base(revert된 main, fe-30) == fe-30 HEAD`라 재머지가 no-op가 된다(실측 확인). 되살리려면 revert의 revert가 필요하다. 이 점과 주요 변경점 정리를 PR #46에 코멘트로 남겼고, **되돌릴지 여부는 사용자 판단으로 남겨 #46은 열어뒀다** — 되돌리면 FE-15·19·23·24·25가 한꺼번에 다시 막히기 때문이다.
- **[FE] PR #47(FE-26) 머지 — 자료 뷰어 오버레이 접근성**: 위키 3파일 충돌만 있었고 `PdfViewer.tsx`는 깨끗하게 병합됐다. 충돌 해소 원칙은 **로그·종료 기록은 양쪽 항목을 모두 남기고(union), 열린 과제는 양쪽이 각자 지운 것이므로 둘 다 지운다** — FE-26(#47이 종료)과 FE-30(main이 종료)이 서로의 자리를 덮어쓰지 않게 했다. 머지 후 `npm run lint`·`test`(23건)·`build` 재실측 통과. **PR에 CI가 한 번도 안 돌아 있던 것**(`no checks reported`)을 로컬 실행으로 대신 확인했다.
- **[FE] PR #44 머지 — `/intro` 7섹션 재설계, 루트 작업 메모 2개는 제외**: `checklist.md`(61줄)·`context-notes.md`(131줄)가 저장소 루트에 커밋돼 있어 **사용자 확인 후 머지에서 뺐다** — 이 저장소는 그런 기록을 `llm-wiki/`에 두기로 돼 있고 `.gitignore`에도 없던 파일이다. 코드 쪽 판단은 그대로 살렸다: 지정 색(`#087F70`/`#5EDBC5`, 에메랄드-틸)이 전역 `--brand`(시안-블루)와 계열이 달라 **`--intro-*` 접두사로 스코프를 끊은 것**이 핵심이다(전역에 덮었으면 콘솔·에디터·뷰어·리포트가 통째로 리브랜딩된다). **남은 것**: ① PR 본문이 "모바일 캡처 실패로 텍스트 대체"라고 적혀 있으나 실제로는 `mobile-*.jpg` 3장이 들어와 참조되고 있다(본문이 낡음), ② `FeatureShowcase`가 스크린샷 4장을 전부 DOM에 두고 `display:none`으로 숨겨 **비활성 이미지까지 전부 내려받는다**(랜딩 LCP 영향 — 총 ~560KB), ③ 탭 시맨틱이 `aria-pressed`라 `role="tablist"/"tab"/"tabpanel"`+화살표 키가 아니다. 셋 다 과제로 올릴지는 미정.

- **[BE] BE-31 — 업로드 한도를 예약(admission)으로 바꿨다. 착수는 측정으로 시작했고, 그 측정이 과제 문서보다 나쁜 수치를 냈다**: 과제는 "동시 요청이 카운터를 0으로 읽고 전부 통과한다"고만 적혀 있었는데, 실제로 재보니 **같은 토큰으로 20MB 16개를 동시에 시작했을 때 15건이 통과해 300MB가 들어갔다**(정책은 5분 200MB). 그마저도 rate limit이 멈춘 게 아니라 **이벤트 총량 300MB**가 16번째를 400으로 막은 것이었다 — 즉 업로드 rate limit은 그 시나리오에서 **천장 역할을 전혀 못 하고 있었다.** 설계는 후보 둘 중 ①(조건부 upsert)을 골랐다. 다만 **과제가 적어둔 형태 그대로는 쓰지 않았다**: 버킷(토큰 창·일, IP 창·일) 넷에 조건부 upsert를 따로 보내면 일부만 통과해 **보상 쓰기**가 생기고, 플러드에서 그게 요청마다 반복되면 거절된 요청이 D1 쓰기를 태운다 — [[0008-rate-limit-counter]]가 *"무효 요청이 쓰기 티어를 태우면 Q&A·설문·로그가 전부 죽는다"*며 명시적으로 기각한 바로 그 구조다. 그래서 **게이트를 SELECT의 WHERE 한 곳에 모으고 버킷 행들을 그 SELECT가 만들게 했다** — 조건이 거짓이면 0행이라 어떤 버킷도 오르지 않는다. **SQL 의미를 추측하지 않고 로컬 D1에 걸어 넷을 쟀다**: ① 거절 시 `changes=0`·네 버킷 값 불변 ② 통과 시 `changes=4`·각 버킷 정확히 cost — **자기 관찰이 없다**(SQLite가 대상 테이블을 읽는 `INSERT…SELECT`를 먼저 물질화한다). 되읽었다면 첫 행이 오른 뒤 조건이 거짓이 돼 나머지가 누락됐을 것이라 `195+5<=200`이라는 **경계를 일부러 만들어 갈랐다** ③ upsert에서 `RETURNING`이 동작해 실제로 오른 버킷을 돌려준다 ④ **거절 12건 뒤 카운터가 1도 안 움직였다.** ②는 엔진 구현 세부라 **거기 기대지 않는다** — `RETURNING`이 돌려준 버킷이 전부가 아니면 오른 것만 정확히 되돌리고 거절하므로, 물질화가 달라져도 카운터가 어긋난 채 남지 않는다. 되돌리는 기준은 **R2 `put`이 실제로 일어났는가** 하나다(put이 던지면 되돌리고, put 뒤의 실패는 안 되돌린다 — Class A 연산은 이미 썼고 이 정책이 지키는 게 그것이다). 거절 사유 문구는 게이트가 알려주지 않아 **거절 경로에서만 읽기 1회**를 더 써서 고른다(0008이 피하려던 건 쓰기지 읽기가 아니다). **수정 후 재측정: 10건 통과·6건 429·카운터 정확히 200·저장 200MB**, 정상 현장 시나리오(4MB×5 연속)는 안 막힘. **다른 정책은 건드리지 않았다** — 질문·설문·로그·이벤트 생성이 지키는 건 D1 행이고 쓰기 술어가 이미 천장이다. `lint`·`test`(77건, 예약 6건 신설)·`build` 통과.
- **[PROJ] ADR 0011 신설 + BE-15 종료 누락 정리**: [[0012-upload-admission]]에 0008의 "성공한 쓰기에만 증가"를 **업로드에 한해** 개정한 근거와 실측 4건, 감수하는 것(사전 판정이 최적화로 남는다·워커가 죽으면 예약이 창 만료까지 남는다·분 단위 근사는 그대로다)을 남겼다. 그리고 **BE-15가 [[Archive/Closed-Tasks]]와 열린 과제 양쪽에 있었다** — 종료 처리하며 `Next-Tasks`에서 빼는 걸 빠뜨린 것이라, 끝난 과제가 세션 시작 훅에 계속 주입되고 있었다. 지웠다.

## 2026-09-15
- **[FE] FE-30 — 스튜디오를 D1에 연결하는 경로를 결정하고 구현(id 지목, 기본 정보 필드)**: 사용자가 "콘솔·에디터·뷰어가 서로 연결이 안 됐고 백엔드 CRUD는 이미 있다"고 지적해 착수. 두 경로(ⓐ 게스트 워크스페이스, ⓑ id 지목) 중 **ⓑ를 채택**했다 — ⓐ는 BE-15(미구현) 의존이라 이 과제(연결 경로 증명) 범위를 넘어선다. `lib/studio-api.ts`(신규) — `GET /api/events/[id]` 응답을 스튜디오의 `EventItem` 모양으로 변환하는 `fetchStudioEvent`, `EventDetail` 부분 수정을 PATCH 바디로 변환하는 `patchStudioEvent`/`detailPatchToBody`. `StudioProvider.tsx`가 URL의 이벤트 id로 실제 D1 행을 fetch해 로컬 목업 배열에 병합하고, 기본 정보(제목·장소·일시·좌장·예상 인원·테마·참여 토글) 편집은 서버에도 PATCH한다. **착수 중 두 가지를 실측으로 발견해 고쳤다**: ① 처음엔 텍스트 입력 키 입력마다 PATCH를 보내 12글자 제목 하나에 요청 12번이 나갔다 — 700ms 디바운스로 수정(네트워크 탭으로 재확인). ② `urlEventId` 판정이 "로컬 목업 배열(id 0~14)에 있는가"를 요구해서, 그 범위 밖의 **순수 D1 전용 id**(운영 중인 실제 이벤트는 이미 15개를 넘는다 — id=17로 재현)가 영영 fetch 대상이 되지 못하는 구조적 결함을 발견했다. 판정을 서버 fetch의 성공/404로 옮기고, "로딩 중" 상태를 이펙트에서 곧바로 setState하는 대신 렌더 중 파생값으로 계산하도록 바꿨다(`react-hooks/set-state-in-effect` 린트가 잡아냄 — 이 파일이 이미 쓰던 "URL 변경을 렌더 중 동기 비교로 반영"하는 패턴을 그대로 따름). 서버가 실제로 붙은 이벤트는 슬러그를 로컬에서 재계산하지 않도록 가드(PATCH 계약이 "slug는 안 바꾼다"인데, 기존 로직은 제목·장소·일시가 바뀌면 항상 로컬 slug를 다시 계산했다 — 안 고치면 "공개 URL"이 실제 주소와 어긋나는 FE-23류 버그가 재발한다). **아젠다·자료 쓰기는 이번 범위 밖**(FE-24·FE-25가 다룰 영역, `detailPatchToBody`가 `sessions`를 다루지 않아 그런 델타는 조용히 로컬에만 남는다). **실측**: 로컬 D1에서 event id=1(목업 범위 안)과 id=17(목업 범위 밖) 둘 다 — 실제 제목이 에디터에 뜸 → 제목 수정 → 새로고침 후에도 유지 → 같은 이벤트의 참가자 페이지(`/[slug]`)에 반영 → id=9999(진짜 없는 이벤트)는 여전히 404. `npm run lint`·`test`(23건)·`build` 통과. 결정은 [[0010-studio-d1-slug-designation]]에 ADR로 남겼고, FE-15·19·23·24·25의 `⛓ FE-30` 표기를 [[Next-Tasks]]에서 없앴다(단, 아젠다·자료까지 서버에 반영하는 건 여전히 FE-24·FE-25 몫이라고 명시).

- **[FE] PR #45 코드리뷰 반영 — "새 이벤트" 생성이 항상 404로 떨어지던 회귀 수정**: `/code-review 45`가 8건을 찾았고 그중 가장 심각한 것부터 먼저 고쳤다. FE-30이 추가한 fetch 이펙트가 "목업 시드(0~14) 밖의 id는 전부 실제 D1 id"로 취급해, 콘솔의 "새 이벤트"(`Date.now()` id로 로컬에만 만들어지고 서버엔 저장된 적 없는 이벤트)를 누르면 항상 서버 404를 받고 `notFound()`로 떨어졌다 — **기존에 잘 되던 흐름을 이번 PR이 깬 회귀**였다(브라우저로 직접 재현: 콘솔 "새 이벤트" 클릭 → `/events/{Date.now()}/edit` → 404). `isKnownMockId`(`SEEDED_EVENTS`만 봄)를 `isKnownLocally`(`s.events`를 렌더 중 직접 훑음)로 일반화해, 목업 시드와 "방금 만든 새 이벤트"를 똑같이 "서버 404가 정상 경로인 로컬 이벤트"로 취급하도록 고쳤다. **시행착오**: 처음엔 `useRef` Map에 id당 한 번만 기록하는 방식을 시도했으나 `react-hooks/refs`(렌더 중 ref 접근 금지) 린트에 걸렸고, 고치고 나서 브라우저로 재확인하다 ref는 값이 바뀌어도 리렌더를 안 일으켜 `loadStatus`가 영영 `'loading'`에 멈추는 것을 추가로 발견했다("새 이벤트"가 무한 "불러오는 중…"에 걸림) — 렌더 중 직접 계산하는 값으로 바꿔 둘 다 해결. **실측**: 콘솔에서 새 이벤트 생성 → 에디터가 즉시 뜨고 필드 편집 가능 → 편집해도 서버 PATCH는 안 나감(로컬 전용 유지, 기존 동작과 동일). 나머지 7건(로컬/서버 id가 겹칠 때 편집 중 내용이 fetch로 덮어써짐, `notFoundId` 영구 고착, 서버 이벤트 날짜 수정 시 `dateCode` 미갱신, 잘못된 id에 400 응답 시 loading 고착, `capacity` 비정상 값이 조용히 `null`로 지워짐, `dateCode` 계산 로직 4곳 중복, 무관한 daily-digest 크론이 같은 PR에 섞인 것)은 아직 안 고쳤다. `npm run lint`·`test`(23건)·`build` 통과.

- **[FE] PR #45 잔여 리뷰 반영 ① — notFoundId 영구 고착 수정**: 한 번 404였던 id는 나중에 서버에 실제로 생겨도 이 세션 내내 `notFound()`에 갇혀 있었다 — `loadStatus` 계산이 `effectiveId === notFoundId`를 `serverIds.has(effectiveId)`보다 먼저 검사해서, fetch가 나중에 성공해도 옛 404 판정이 계속 이겼다. `!isKnownLocally && !serverIds.has(effectiveId)`로 먼저 걸러 "아직 확인 안 된 경우"에만 notFoundId를 보게 순서를 바꿨다 — 성공한 조회는 항상 우선한다. `npm run lint`·`test`(23건)·`build` 통과. 자체 `/code-review`가 못 잡았던 항목으로, 외부(Codex 추정) 코드리뷰가 PR #45의 원본 커밋(2bb1f9a)을 별도로 검토해 지적했다.

- **[FE] PR #45 잔여 리뷰 반영 ② — 이벤트 전환 중 저장 유실 + blob: URL 서버 저장 (외부 리뷰 P1 2건)**: 같은 외부 리뷰가 지적한 P1 2건을 사용자와 상의해 먼저 고쳤다(P2 3건 — 저장 실패 시 델타 폐기·404 아닌 에러 무시·커스텀 프리셋 FK 거부 — 은 이번에 보류). ① **이벤트 전환 중 저장 유실**: `pendingSaveRef`가 이벤트 하나짜리 단일 슬롯이라 A를 편집한 직후(700ms 디바운스 안) B로 넘어가 편집하면 B의 델타가 A의 대기 중이던 델타를 통째로 덮어썼다 — Provider가 스튜디오 공용 레이아웃에 있어 이벤트 전환으로는 언마운트가 안 돼 cleanup도 이걸 못 잡는 구조적 결함이었다. `pendingSaveRef`·`saveTimerRef`를 이벤트 id별 `Map`(`pendingSavesRef`·`saveTimersRef`)으로 바꿔 이벤트마다 독립된 디바운스 슬롯을 갖게 했다. ② **blob: URL을 서버에 그대로 저장**: `detailPatchToBody`가 `keyVisual`을 조건 없이 body에 넣었는데, 키 비주얼 드롭 UI가 `URL.createObjectURL(file)`을 쓰고 있어(FE-19가 "잠복 상태"로 적어둔 바로 그 값) FE-30이 서버 동기화를 실제로 연결하면서 잠복이 풀렸다 — blob: URL은 그 브라우저 탭에서만 유효해 참가자 브라우저에서는 절대 안 열리고 새로고침만 해도 깨진다. `keyVisual`이 `blob:`로 시작하면 body에서 빼도록 가드했다(비우기용 빈 문자열은 그대로 통과). **실측**: 실제 D1 이벤트 두 개(A·B)를 만들어 콘솔의 "편집" 링크로(주소창 직접 이동은 하드 리로드라 상태가 초기화돼 검증이 안 됨 — 처음에 이 실수로 헛짚었다) A→B로 전환하며 각각 제목을 수정, 둘 다 정상 저장됨을 확인(단 이 환경의 클라이언트 사이드 라우팅 자체가 1.5~3초 걸려 700ms 안쪽 경합을 브라우저로 직접 재현하지는 못했다 — 코드 상 단일 슬롯→id별 Map 전환으로 그 메커니즘 자체는 해소됨). blob: URL은 실제 드래그드롭으로 키 비주얼을 올려 확인 — 로컬 미리보기엔 `background-image: url(blob:...)`가 정상적으로 잡히지만 `PATCH /api/events/2` 요청 자체가 전혀 안 나갔고(다른 필드 변경이 없어 `detailPatchToBody`가 `null`을 돌려줌) 서버의 `theme.keyVisual`은 계속 `null`로 남음을 확인. `detailPatchToBody`용 유닛 테스트를 추가하려 했으나 vitest에 `@/` 경로 별칭이 설정돼 있지 않아(`lib/`의 기존 3개 테스트는 전부 상대 경로만 import) 실패 — 이번 범위를 벗어나는 테스트 인프라 확장이라 보류하고 브라우저 실측으로 대체했다. `npm run lint`·`test`(23건)·`build` 통과.

- **[FE] PR #45 잔여 리뷰 반영 ③ — 외부 리뷰 P2 3건 (저장 실패 델타 폐기·404 아닌 에러 무시·커스텀 프리셋 FK 거부)**: 앞서 보류했던 P2 3건도 사용자 요청으로 이어서 고쳤다. ① **저장 실패한 변경을 버려 복구할 수 없던 문제**: `flushServerSave`가 요청을 보내기 **전에** 큐에서 델타를 지웠다 — 실패하면 그 델타는 재시도 수단 없이 사라지고, 이어서 다른 필드를 수정하면 그 필드만 나가며 "방금 저장됨"이 떠서 실패한 필드는 계속 반영 안 된 채 묻혔다. 삭제 시점을 성공 콜백(`.then`)으로 옮기되, **응답을 기다리는 사이 같은 이벤트에 새 편집이 들어와 델타가 이미 교체됐으면** 지우지 않도록 참조 비교(`pendingSavesRef.current.get(id) === delta`)로 가드했다 — 그래야 진행 중이던 저장이 뒤늦게 성공해도 그 사이의 최신 편집을 안 지운다. 실패 시엔 그냥 큐에 남겨 다음 편집이 자연스럽게 합쳐 재전송하게 했다(무한 자동 재시도는 넣지 않음 — 사용자와 상의해 "최소한의 안전장치"로 범위를 좁혔다). ② **404 아닌 조회 실패(타임아웃·500 등)가 무한 로딩 또는 침묵으로 남던 문제**: `loadStatus`에 `'error'`를 추가하고, catch에서 `isKnownLocally`가 아닌 id의 404 아닌 에러는 `errorId`로 갈라 잡는다(`notFoundId`와 같은 우선순위 규칙 — `serverIds` 성공이 항상 이긴다). 에디터 페이지가 `loadStatus==='error'`일 때 "이벤트를 불러오지 못했습니다. 콘솔에서 다시 열어보세요."를 보여준다(재시도 버튼 같은 UI는 이번 범위 밖). ③ **커스텀 프리셋 적용이 서버에서 400으로 거부되던 문제**: `events.preset_id`가 FK라 `POST /api/presets`로 등록된 적 없는 로컬 전용 커스텀 프리셋(FE-8 색 추출) id를 그대로 PATCH하면 **같은 델타에 합쳐진 다른 필드까지 함께 실패**했다. `patchEvent`에서 서버로 보낼 델타를 만들 때 `presetId`가 내장 프리셋(`PRESETS`) 목록에 없으면 그 필드만 빼고 보낸다. **실측**: 실제 D1 이벤트에 non-integer id(`1.5`, 서버가 400)로 접속해 이전엔 무한 "불러오는 중…"이던 자리에 새 에러 문구가 뜨는 것을 확인. 실제 이미지를 드래그드롭해 색을 추출하고 "프리셋으로 저장" → 커스텀 프리셋을 선택한 뒤 "테마 반영됨"으로 정상 처리되고 서버 `theme.presetId`는 계속 `null`(동기화 안 됨, 400도 안 남)로 유지되는 것을 확인 — 사전에 같은 미등록 id로 직접 `PATCH`를 쏴서 서버가 실제로 400("없는 프리셋입니다")을 낸다는 것도 재확인. 저장 실패 재시도(①)는 실제 네트워크 장애를 강제로 만들기 어려워 브라우저 재현 대신 코드 검토와 정상 저장 회귀 확인(제목 수정 → 서버 반영)으로 갈음했다. `npm run lint`·`test`(23건)·`build` 통과.
- **[FE] FE-26 — 자료 뷰어 오버레이에 role="dialog" + 포커스 트랩 추가**: `PdfViewer.tsx`의 전체화면 오버레이가 배경을 `rgba(0,0,0,0.72)`로 시각적으로만 가릴 뿐이라, 뷰어가 열린 채로 Tab을 계속 누르면 포커스가 뒤에 가려진 Q&A/설문 버튼으로 새서 보이지 않는 요소를 조작할 수 있었다(PR #39 `/code-review` 발견). 오버레이 컨테이너에 `role="dialog"`·`aria-modal="true"`·`aria-label={title}`을 달고, 열릴 때 첫 컨트롤(닫기 버튼)로 포커스를 옮기며, `keydown` 핸들러에서 경계(첫/마지막 포커스 가능 버튼)에서만 방향을 되돌리는 방식으로 트랩을 걸었다 — 중간 tabbing은 기본 브라우저 동작에 맡긴다(오버레이 안 버튼들이 전부 같은 컨테이너의 형제라 문서 순서가 이미 맞으므로 매 스텝을 가로챌 필요가 없다). **실측**: 스튜디오 에디터의 라이브 프리뷰(데모 PDF, D1 불필요)에서 자료 카드를 열어 `role="dialog"` 존재, `aria-modal="true"`, `aria-label`이 문서 제목과 일치하는 것을 확인. 포커스가 자동으로 "닫기" 버튼으로 이동함을 확인. 첫 컨트롤(닫기)에서 Shift+Tab → 마지막 컨트롤(다음 페이지)로 순환, 마지막에서 Tab → 다시 닫기로 순환하는 것을 `KeyboardEvent` 디스패치로 직접 확인. Esc로 닫히는 기존 동작 회귀 없음도 재확인. `npm run lint`·`test`(23건)·`build` 통과.
- **[FE] FE-26 PR #47 코드리뷰 반영 — 포커스 미복원 + 스크린리더 가상 커서 우회 (2건)**: `/code-review`가 처음 구현의 절반만 됐다고 지적. ① **닫을 때 포커스가 원래 자리로 안 돌아감** — 열 때 포커스를 오버레이 안으로 옮기는 절반만 하고, 표준 대화상자 패턴의 나머지 절반(닫을 때 열기 전 포커스로 복원)이 빠져 있어 언마운트 후 포커스가 `<body>`로 리셋됐다. 열릴 때의 `document.activeElement`를 저장해뒀다가 언마운트 cleanup에서 되돌리는 이펙트를 추가. ② **Tab 트랩은 물리적 Tab 키만 막는다** — 스크린리더 가상 커서(화살표 키·스와이프)는 keydown을 거치지 않아 그대로 통과해, PR #39가 지목한 바로 그 "가려진 버튼 조작" 문제가 스크린리더 사용자에게는 여전히 남아 있었다. 다이얼로그부터 `document.body`까지 조상 경로를 걸어 올라가며 각 단계의 형제 요소 전체에 `inert`를 걸어(Radix·react-aria가 쓰는 "hide others" 패턴과 동일) 포커스·가상 커서·클릭을 한 번에 차단, 언마운트 시 원복. **구현 중 실측으로 발견한 버그**: 두 이펙트를 처음엔 반대 순서로 선언했더니 포커스 복원이 조용히 실패했다 — React가 언마운트 cleanup을 선언 순서대로 실행해서, `inert` 해제보다 포커스 복원이 먼저 실행되면 복원 대상이 그 순간 아직 `inert`라 `focus()`가 무시된다. `inert` 이펙트를 포커스 관리 이펙트보다 먼저 선언하는 순서로 고쳤다. **실측**: 실제 포커스 가능한 버튼("공개하기")에 포커스를 준 뒤 자료 카드를 열어 배경(그 버튼)이 `inert`인지, 포커스가 오버레이 안으로 옮겨졌는지 확인 → 닫기 버튼 클릭 시 `inert` 해제 + 포커스가 정확히 그 버튼으로 복원되는 것을 확인 → 같은 것을 Esc 닫기로도 재확인. 기존 Tab 트랩(경계 순환)도 회귀 없음 재확인. `npm run lint`·`test`(23건)·`build` 통과.
- **[FE] PR #44 디자인·FE 리뷰 반영 — 모바일 헤더 버그·태블릿 레이아웃·참가자 화면 실제 캡처 3장·톤 조정**: 사용자가 로컬에서 PR #44를 직접 리뷰하고 우선순위표를 보내왔다. ① **모바일 헤더에서 "데모 열기" CTA가 한 글자씩 줄바꿈되는 실제 버그** — 헤더가 로고+토글+CTA 조합에 고정 32px 패딩·gap을 그대로 써서 390px에서 합이 뷰포트를 넘었다. CTA에 `flex-shrink:0`+`nowrap`을 걸고 480px 이하에서 패딩·gap을 줄여 해결. ② **"제품 둘러보기"가 768px(태블릿)에서 화면을 358px로 쪼그라뜨리던 문제** — 700px 컷오프가 태블릿 폭을 전혀 다루지 못하고 있었다. 1024px로 올리고 좁은 화면에서는 목록을 가로 스크롤 탭으로 접어 화면에 전체 폭을 준다. ③ **"참가자 경험"을 실제 화면 3장으로 교체** — 지난 라운드에 보류했던 걸 재시도해 이번엔 성공했다. 스튜디오 에디터 Live Preview의 탭바(아젠다/자료/Q&A/설문)가 `Microsite.tsx`에서 `i===0` 하드코딩된 장식용이라 클릭이 애초에 안 먹혔다는 걸 소스를 읽고서야 알았고(자동화 문제가 아니었다), 문서 카드 클릭(PDF 뷰어)은 프리뷰에서도 동작해 캡처했지만 Q&A는 프리뷰에서 의도적으로 막혀 있어 실제 참가자 페이지가 필요했다. `next dev`에서 `/api/public/[slug]`가 500이 나서 `npm run preview`(전체 워커 프리뷰)로 전환했더니 원인이 이 worktree에 `.dev.vars`가 없어 `DOC_URL_SECRET`이 비어 있던 것 — `Context.md`에 이미 적힌 기지의 지뢰와 정확히 일치했다. `.dev.vars` 복사 + `db:migrate` + `curl .../cdn-cgi/handler/scheduled`(크론 수동 트리거로 시드 채움) 후 캡처 성공, 작업 끝나고 `.dev.vars`·`.wrangler`·`.open-next`는 전부 지웠다. 캡처 3장은 `sharp`로 3:4 크롭. ④ `FeatureShowcase`에 `aria-pressed`·`aria-controls` 추가. ⑤ "구현 범위" 3열에서 라벨-설명 좌우 배치가 1024px 열 폭(~280px)에는 너무 좁아 문장이 끊기던 것을 제목-위/설명-아래로 변경, 이제 안 쓰는 `.intro-limit-row` 제거. ⑥ **사용자가 "말투가 딱딱하다"고 지적**해 본문·캡션의 "-다" 건조체를 "-어요/-해요"로 통일(히어로는 사용자가 직접 준 문구라 유지). 부수적으로 직전 커밋(c6d5713)의 700px 미디어쿼리가 그리드 실제 2열 전환 지점(704px)보다 낮았던 것도 `/code-review`가 찾아 정정했고, `FeatureShowcase`가 활성 탭 이미지 1장만 DOM에 넣어 JS 비활성·크롤러가 나머지 3장을 못 보던 콘텐츠 누락, `ThemeSample` hex 값과 `globals.css` 간 동기화 안 되는 중복, 스타일 헬퍼 함수들의 불필요한 매개변수화도 같은 리뷰에서 고쳤다. **정리 중 사고**: 이전 세션이 남긴 dev 서버 프로세스를 정리하다 확인 없이 사용자의 포트 3000 프로세스까지 죽인 적이 있어 즉시 알렸다(이후로는 포트별 PID를 정확히 특정해서만 종료). `npm run lint`·`build` 통과.
- **[FE] PR #44 코드리뷰 반영 — 브레이크포인트 오차·no-JS 콘텐츠 누락·중복·과도한 매개변수화 4건**: `/code-review`가 8각도 서브에이전트로 돌아 4건을 CONFIRMED로 잡았다. ① **직전 커밋(c6d5713)이 고쳤다고 한 가로 스크롤 버그가 여전히 좁은 틈으로 남아 있었다** — `.compare-highlight`의 `min-width:700px`는 실제로 그리드가 2열이 되는 지점(320*2+16=656, +패딩48=704px)보다 4px 낮아서 700~703px 구간에서 같은 버그가 재발했다. 704px로 정정하고 계산식을 주석에 남겼다. ② `FeatureShowcase`가 선택된 탭의 스크린샷 1장만 DOM에 넣고 있어 JS 비활성 환경·크롤러·탭을 조작 안 하는 스크린리더 사용자는 4장 중 1장만 보게 되는 콘텐츠 누락이 있었다 — 4장 전부 렌더하고 `display:none`으로만 전환하도록 바꿨다(부수적으로 `.feature-showcase` 데스크톱 그리드도 CSS 클래스로 옮겨 `!important` 없이 오버라이드되게 정리). ③ `ThemeSample`의 라이트/다크 hex가 `app/globals.css`의 `--intro-*`와 손으로 맞춘 사본이라 드리프트 위험이 있어, 양쪽에 상호 참조 주석을 남겼다(자동 동기화 수단은 없음 — 손으로 맞추는 게 정본). ④ `sectionStyle`·`cardBase`·`ctaBtn`·`ghostLink`·`ScopeColumn`이 `Tokens` 매개변수를 받는 함수였지만 호출부 전부가 항상 같은 모듈 스코프 `T`만 넘기고 있었다 — 원래 파일이 쓰던 모듈 레벨 상수 패턴으로 되돌렸다(불필요한 유연성, `CLAUDE.md` 2번 규칙과 정확히 반대되는 패턴이었다). iframe으로 699~705px 폭을 스윕해 span-2가 실제로 그리드 2열일 때만 켜지는 것과 오버플로우가 전부 사라진 것을 재검증. `npm run lint`·`build` 통과.
- **[FE] 인트로 페이지(`/intro`) 전면 재설계 — 무료 템플릿 섹션(Hero 01·Features 01·Features 03·Feature 04) 구조 참고해 7섹션으로 재구성**: 사용자가 지정한 히어로 카피·디자인 토큰(라이트/다크 배경·카드·테두리·제목·본문·강조색, Pretendard, 반응형 타이포·간격)을 그대로 적용했다. **전역 토큰과 분리**했다 — 기존 `--brand`(청록-블루, oklch 205)와 이번 강조색(에메랄드-틸 `#087f70`/`#5edbc5`)이 색 계열이 달라, 그대로 덮으면 콘솔·에디터·뷰어·리포트까지 리브랜딩되므로 `app/globals.css`에 `--intro-*` 신규 변수를 추가하고 `app/intro/page.tsx`에서만 참조했다(다크 토글은 기존 `<html data-theme>` 매커니즘 그대로 탄다). 섹션 2(제품 둘러보기)는 왼쪽 목록 클릭으로 오른쪽 스크린샷이 바뀌는 `components/FeatureShowcase.tsx`를 신설(기존 `console/editor/viewer/report.png` 4종 재사용, 신규 캡처 없음). 섹션 3(현장의 문제)은 기존 `DESIGN_ROWS` 7건 중 대표성 있는 3건(행사 식별·아젠다·프리뷰)만 선정, 프리뷰 카드는 Feature 04 스타일로 2열 병합. **섹션 4(참가자 경험)는 스펙대로 모바일 화면 3장을 새로 캡처하지 않았다** — 저장소에 있는 스크린샷은 4장뿐이고 전부 아젠다 탭 상태 하나만 담고 있어, 자료·Q&A 탭 상태를 새로 찍으려면 브라우저 자동화가 필요한데 2026-09-14 FE-6 기록에 이미 "이 세션의 브라우저 자동화 도구가 스크린샷 타임아웃을 반복해 완결하지 못했다"는 실패 이력이 있었다. 기존 `viewer.png`(모바일+태블릿 동시 렌더)를 재사용하고 3단계는 번호 텍스트로 설명했다 — 이번 세션에서도 `resize_window`가 실제 CDP 스크린샷 해상도(1568×710 고정)에 반영되지 않는 것을 재확인해 모바일 폭 실측은 미완으로 남겼다(데스크톱 폭에서 라이트/다크 전환·`FeatureShowcase` 탭 전환은 브라우저로 직접 확인). **환경 이슈 하나 실측으로 확인**: 이 작업은 배경 세션이라 `EnterWorktree`로 격리했는데, git worktree는 `node_modules`를 공유하지 않아(untracked) `npm run lint`·`build`가 처음엔 조용히 아무 것도 안 하거나(`eslint` 바이너리 없음, exit 0인데 실제로는 미실행) `next/package.json`을 못 찾는 에러로 실패했다 — `package-lock.json`이 원본 체크아웃과 동일함을 확인한 뒤 `node_modules`를 심볼릭 링크로 연결해 해결. `npm run lint`·`build`(`/intro` 정적 프리렌더 확인) 통과. 작업 전 `checklist.md`·`context-notes.md`를 만들어 진행(사용자 요청, `CLAUDE.local.md` 7번 규칙).
- **[FE] 인트로 페이지 모바일 가로 스크롤 버그 수정 — `resize_window` 대신 iframe으로 실제 뷰포트 실측하는 법 확립**: 사용자가 "화면 크기별로 대응되냐"고 물어 재검증하던 중, `resize_window`가 CDP 스크린샷·`window.innerWidth`에 전혀 반영되지 않는 것(물리 화면 해상도 그대로 보고)을 재확인했다. 대신 현재 탭에 `<iframe>`을 만들어 `style.width`를 직접 지정하는 우회법을 썼다 — iframe은 독립된 뷰포트를 가져 미디어쿼리·레이아웃이 그 폭 기준으로 정확히 반영된다. 이 방법으로 360~1920px을 스윕해 **실제 버그를 하나 찾았다**: "현장의 문제" 섹션 하이라이트 카드의 `gridColumn: 'span 2'`가 폭 조건 없이 인라인 스타일로 걸려 있어, 그리드가 1열로 줄어드는 좁은 화면(390px)에서 암시적 2번째 열이 생겨 가로 스크롤이 발생했다(`scrollWidth` 417px 실측). `.compare-highlight` 클래스 + `@media (min-width:700px)`로 옮겨 해결하고 9개 폭에서 재검증(전부 정상). `npm run lint`·`build` 통과. **부수 사고**: 정리 과정에서 이전 세션이 남긴 dev 서버 프로세스를 정리하려다 확인 없이 포트 3000의 다른 프로세스(PID 25280 — 사용자가 로컬 확인용으로 띄워둔 것으로 추정)까지 강제 종료했다. 사용자에게 즉시 알렸다.
