# sympo-studio — Log

이 프로젝트의 시간순 기억. 최신이 위로.

> **형식 계약 (훅이 파싱한다)**: 날짜 섹션은 `## YYYY-MM-DD`, 항목은 `- **제목**: 내용`.
> 코드 repo의 SessionStart 훅이 최신 섹션의 **제목**들만 추출해 세션에 주입한다.
> 제목은 주입만 보고도 무슨 일이 있었는지 알 수 있게 쓴다.

## 2026-08-18
- **FE-12 — 인트로 페이지 반응형 + 랜딩 재설계**: `design.md` 6·7절 기준으로 실제 구현. 그리드는 `auto-fit` + `minmax(min(Npx,100%),1fr)` 패턴으로 미디어쿼리 없이 반응형 처리 — 375px 실측 중 고정 `minmax(480px,1fr)` 등이 좁은 컨테이너보다 바닥값이 커서 **가로 오버플로를 일으키는 것을 발견**(`document.documentElement.scrollWidth`로 진단 스크립트를 짜서 정확히 어느 요소인지 특정), `min(Npx,100%)`로 바닥을 캡해 해결하고 나머지 grid에도 방어적으로 적용. grid로 못 푸는 두 곳(헤더 앵커 내비 숨김, "알고 있는 한계" 라벨 줄바꿈)만 `globals.css`에 미디어쿼리 추가. "설계 판단" 7행 표를 Before→After 비교 카드로 교체해 표 문제와 반응형 문제를 동시에 해결하고, 결론 인용구를 브랜드 소프트 배경의 큰 텍스트로 격상해 "PPT 같다"는 지적의 핵심이었던 스케일 대비 부재를 보완. 375/820/1440px 3개 뷰포트 헤드리스 스크린샷 + 콘솔 에러·가로 오버플로 0건 확인. lint·build 통과. `Next-Tasks.md`의 FE-12에 결과 기록. 커밋 안 함(사용자 확인 대기).
- **셸+인트로 다크모드 구현 + `llm-wiki/design.md` 신설**: 사용자가 "기본은 라이트, 다크모드도 되게" 요청(대상: 셸+인트로 둘 다). `lib/ui.ts`의 `#fff`·산발적 회색 리터럴 90여 곳을 조사해 CSS 변수 기반 토큰(`app/globals.css`의 `:root`/`:root[data-theme='dark']`)으로 흡수, `ThemeToggle`(`useSyncExternalStore` 사용 — effect 내 setState 린트 규칙 회피)로 라이트↔다크 토글 + `localStorage` 지속을 구현. `layout.tsx`에 페인트 전 스크립트 + `suppressHydrationWarning`으로 하이드레이션 경고 제거(헤드리스 브라우저로 실측 확인). 인트로 페이지는 기존의 독자적 다크 전용 팔레트(hue 220)를 버리고 셸과 같은 토큰 체계로 통합 — 결과적으로 인트로도 라이트 기본으로 전환. 상태 배지 등 의미색은 다크 페어 없이 남겨 알려진 갭으로 명시.
  이어서 사용자가 "인트로 페이지가 PPT 같다" + "반응형이어야 한다"고 피드백, 프로젝트 전체 디자인 컨셉 문서(`llm-wiki/design.md`) 작성을 요청 — 범위는 "프로젝트 전체"로 사용자가 직접 선택. 색 시스템 분리 원칙(도구 색 vs 행사 색), 타이포·레이아웃 규칙, **화면별 반응형 전략**(셸=데스크톱 전용/마이크로사이트=모바일·태블릿/인트로=완전 반응형 필요)을 표로 정리하고, 인트로 재설계 원칙(스케일 대비·표 최소화·여백)을 근거와 함께 남겼다. `Next-Tasks.md`에 **FE-12**(인트로 반응형 + 랜딩 재설계)로 등록, `index.md`에 `[[design]]` 링크 추가. 코드 구현(FE-12 실제 반응형 작업)은 다음으로 미룸 — 이번엔 컨셉 문서까지.
- **행사 페이지·소개 페이지 위치 안내**: 사용자가 "관리자 페이지만 되어 있는 거 아니냐"고 물어, 행사(참가자) 페이지는 처음부터 `Microsite.tsx`로 존재하며 에디터 라이브 프리뷰와 뷰어 화면에서 확인 가능하고, 소개 페이지는 전날 만든 `/intro`(`design/intro-page` 브랜치, PR #6)에 있다고 안내. 아직 FE-2(라우트 분리) 전이라 행사 페이지가 독립 URL이 아니라 스튜디오 앱 내 화면 전환으로만 보인다는 점을 명시. 코드 변경 없음.

## 2026-08-17
- **FE-10 커밋·push·PR — fork 경유로 진행**: `design/brand-identity`를 커밋한 뒤 `origin`(fomula91/sympo-studio)에 push가 403으로 막힘 — 이 세션의 GitHub 인증(`huni2`)이 저장소 소유자(`fomula91`)와 다른 계정이라 쓰기 권한이 없었다. `huni2`가 이미 가진 fork(`huni2/sympo-studio`)로 push한 뒤 fork→원본으로 [PR #5](https://github.com/fomula91/sympo-studio/pull/5)를 열어 해결. 기존 PR #1~4는 원본 저장소에 직접 push된 브랜치였다는 점이 다르다는 걸 PR 본문에 명시. `Next-Tasks.md`의 FE-10 완료 기준을 PR 링크로 갱신.
- **`design/intro-page` 브랜치 — 다크 인트로 페이지(FE-11) 구현**: `design/brand-identity` 기반으로 새 브랜치를 파서 `/intro` 라우트를 만들었다. 디자인 재현 프롬프트 06 스펙을 그대로 베끼지 않고, **문제 정의 문구는 `docs/problem-redefinition`(PR #2, 미병합)의 정정된 README를 근거로 썼다** — main의 README는 field-experience.md로 3/4가 허구였다고 밝혀진 옛 문제 정의라 그대로 쓰면 이미 정정된 거짓 주장을 새 페이지에 다시 심는 셈이었다. 사용자에게 이 판단을 먼저 확인받음. 콘솔·에디터·뷰어·리포트 스크린샷 4장을 헤드리스 브라우저로 실제 캡처해 `public/screens/`에 저장, 06 스펙의 정적 목업 대신 실화면을 썼다. 구현 중 두 가지 버그를 스크린샷 검토로 발견·수정: ① `LogoLockup`이 라이트 셸 색(`UI.ink`)을 고정으로 써서 다크 배경에서 워드마크가 안 보임 — `ink`/`sub` prop을 받도록 고침. ② CTA 헤드라인에 빨강/청록 색번짐이 보였으나 `font-smoothing` 등 CSS로 재현·수정되지 않아 실제 브라우저가 아닌 헤드리스 스크린샷 도구의 렌더링 아티팩트로 판단, 코드는 되돌림. lint·build 통과, 콘솔 에러 0건. `Next-Tasks.md`에 FE-11로 등록(FE-8·FE-9는 `docs/problem-redefinition`에 이미 있어 충돌 피함).
- **`design/brand-identity` 브랜치 — 스튜디오 셸 브랜드 컬러 토큰 적용(목업)**: 사용자가 제공한 디자인 재현 프롬프트(OKLCH 205도 틸, 프리셋 5종과 hue가 겹치지 않는 값)를 근거로 새 브랜치를 파서 반영. `lib/ui.ts`에 `brand`/`brandPress`/`brandSoft` 토큰과 `LogoMark`/`LogoLockup`(`components/Logo.tsx`, div 3개로 구성한 로고 마크) 추가. 스튜디오 셸(`StudioApp.tsx` 내비·헤더 버튼), 콘솔(`ConsoleScreen.tsx` 정렬·상태필터·카드 선택), 에디터(`EditorScreen.tsx` 섹션 내비·프리셋 카드·기기 토글), 리포트(`ReportScreen.tsx` AA 통과율 카드·세션 바·운영 지표)에 브랜드색을 "선택 상태·프라이머리 액션에만" 규칙으로 반영. 참가자 마이크로사이트(`Microsite.tsx`)는 기존 구현이 이미 스펙과 일치해 손대지 않음 — 스튜디오 브랜드색이 섞이지 않고 이벤트 자체 테마(예: AURORA 그린)만 쓰는 것을 헤드리스 브라우저 스크린샷으로 실측 확인. lint·build 통과, 콘솔 에러 0건. 다크 인트로 페이지(스펙 06)는 라우트 분리 여부를 사용자와 상의 후 이어가기로 하고 이번 세션에서는 보류. 커밋은 하지 않음(사용자 확인 대기).

## 2026-08-16
- **브랜치·PR 워크플로우 실측 확인 + FE 온보딩 문서 존재 파악**: 프론트엔드 담당자로 합류한 사용자가 "기능마다 브랜치 파서 PR 날리는 방식이 맞냐"고 물어, GitHub 열린 PR 4건(`feat/be-1-d1-schema`, `docs/log-tags`, `docs/problem-redefinition`, `docs/discord-webhook`)을 `gh pr list`로 확인해 브랜치+PR 패턴을 실측으로 답했다. 그 과정에서 PR #3(`docs/log-tags`, 아직 미머지)에 `llm-wiki/Onboarding-FE.md`가 이미 작성돼 있음을 발견 — 로그 담당 태그(`[FE]`/`[BE]`/`[PROJ]`) 도입과 세트로, main에는 아직 반영되지 않은 상태임을 사용자에게 안내했다. 코드 변경 없음(조사·설명 세션).

## 2026-08-15
- **Next-Tasks를 FE/BE로 분리하는 룰 추가 (ADR 0003)**: 열린 과제가 7건으로 늘면서 "브라우저만 열면 되는 일"과 "D1·Workers를 띄워야 하는 일"이 섞여, 세션 시작 시 주입되는 제목만으로는 착수 비용을 가늠할 수 없었다. `## 열린 과제 — 프론트엔드(FE)` / `## 열린 과제 — 백엔드(BE)` 두 섹션 + `FE-N.`/`BE-N.` 제목 접두사로 결정(섹션 분리만으로는 훅이 헤더를 버려 주입 결과에서 구분이 사라지므로 **둘 다** 필요). 훅의 awk(`.claude/settings.json:8`)는 `/^## 열린 과제/{f=1;next}`라 같은 접두사 섹션이 둘이면 양쪽 다 추출되고 `## 종료 기록`에서 멈춘다 — 스크래치패드에서 FE 2건 + BE 1건 + 미끼 항목으로 실측 확인, **훅 수정 불필요**. 가로지르는 과제는 쪼개고 FE 쪽에 BE 의존을 명시, 번호는 각각 독립이며 재번호 금지(로그·ADR 참조가 깨진다). 규칙 본문은 `CLAUDE.md`의 「Next-Tasks 작성 규칙」. 이어서 **`Next-Tasks.md`를 새 룰대로 재편**: 기존 7건 중 가로지르던 5건을 쪼개 **FE 7 + BE 6 = 13건**으로. 순수 FE는 대비비 게이트(FE-7)뿐이었고, 배포·Q&A·설문·OPS·PDF는 전부 가로지르고 있었다. 쪼개면서 **FE↔BE 양방향 의존**이 드러남 — BE-2(배포)의 완료 기준이던 "4개 화면 딥링크"는 실제로 FE-2(라우트 분리)가 해야 하는 일이라 완료 기준을 각 섹션으로 옮겼다. 실제 파일로 훅 awk 재검증: 13건 정상 추출, 종료 기록 표 미포함. **서버 없이 지금 착수 가능한 것은 FE-2·FE-7과 FE-4의 수료증 부분**이며 문서 상단에 명시.
- **SessionStart 훅 파서를 접두사 기준으로 교체 + fail-loud**: FE/BE 분리는 기존 awk로도 동작했지만, 계약이 **섹션 제목이라는 사람이 쓰는 문자열**에 걸려 있어 `## 프론트엔드 과제`처럼 바꾸는 순간 에러 없이 0건이 되는 조용한 실패를 안고 있었다(같은 미끼 파일로 비교 실측 — 기존 0건, 새 방식 정상). 파서를 `awk '/^### (FE|BE)-/{print}'` 한 줄로 교체해 이미 형식 계약인 **제목 접두사**를 직접 읽도록 하고, 추출이 0건이면 형식 오류 안내 문구를 대신 주입하도록 했다. 부수 효과로 섹션 제목이 자유로워지고 종료 기록 아래 `###` 줄은 접두사가 없어 자동 제외된다. 저장 후 `jq -e` 스키마 검증 + settings.json에서 명령을 꺼내 실제 실행해 13건 주입 확인. `CLAUDE.md`·`Next-Tasks.md`·ADR 0003의 형식 계약 서술을 함께 갱신.
- **실제 동작하는 데모로 방향 전환 (ADR 0001·0002)**: 정적 전용(localStorage)으로는 Q&A·설문이라는 제품 핵심을 증명할 수 없다고 판단해 백엔드를 붙이기로 결정. 회원가입은 요구하지 않고(채용담당자는 가입하지 않는다), 공개 쓰기는 rate limit·글자 수 제한·매일 시드 리셋으로 통제하며, 콘솔·에디터는 서버 없이도 동작하는 하이브리드 실패 모드를 유지한다. 스택은 Cloudflare 단일 벤더(Workers + D1 + R2), 원칙은 **"저장·조회는 서버, 렌더링·생성은 클라이언트"** — 무료 티어가 인색한 자원은 저장 공간이 아니라 CPU 시간이라서다(PDF는 PDF.js 클라이언트 렌더링, 수료증은 `pdf-lib` 클라이언트 생성). 견적상 월 방문자 500명 기준 한도의 1~2%만 사용. `Next-Tasks`를 의존 순서대로 7건으로 재편하고, 정적 export 과제는 폐기 처리.
- **README 재작성**: create-next-app 기본값을 프로젝트 문서로 교체. 문제 정의 → 설계 판단 표(Before/After/왜) → 다시 만들면서 바꾼 것 → 기술 선택과 트레이드오프 → **알고 있는 한계** → 가상 데이터 고지 구성. 회사명 없이 "실무에서 관찰한 문제를 처음부터 다시 설계한 개인 프로젝트" 프레이밍. 리포트가 샘플임을 README와 UI 배지(`ReportScreen`) 양쪽에 명시. lint·build 통과.
- **새 리포지토리 이전**: 원본 데이터가 남은 커밋 히스토리를 버리기 위해 기존 public 리포를 삭제하고 동일 이름(`fomula91/sympo-studio`)으로 재생성. 로컬 `.git` 초기화 → 단일 초기 커밋으로 시작(이후 `Co-Authored-By: Claude` 트레일러 제거로 재작성되어 현재 `cd1039c`). 원격을 다시 clone해 `git log -S`로 원본 문자열 조회 0건 검증(`package-lock.json`의 sha512 해시에 `T2D`·`CKM`이 우연히 포함된 2건은 오탐). 삭제 전 `.git` 백업은 세션 스크래치패드에 보관.
- **가상 데이터 전환**: 포트폴리오 공개를 위해 실존으로 보이는 의료인·소속기관·의약품·행사장 데이터를 전부 가상 세트로 교체. `lib/data.ts` 외에 `Microsite.tsx`(FILES·fallback), `StudioApp.tsx`(INITIAL), `ReportScreen.tsx`(하드코딩 문자열)에 흩어져 있던 것까지 정리. lint·build 통과. **커밋 aa065d4의 diff에 원본이 남아 있어 새 리포지토리 이전 필요**(→ Next-Tasks).
- **방향 전환 — 포트폴리오용 무료 영구 배포**: 전 직장 담당 프로젝트를 포트폴리오로 마이그레이션하는 것이 목표. "오랫동안 무료"의 적은 비용이 아니라 무료 티어 정책 변경·비활성 pause라고 판단 → 런타임 의존성 0(정적 export + Cloudflare/GitHub Pages + localStorage) 방향으로 결정. DB·Auth·Realtime은 도입하지 않음.
- **하네스 설치**: LLM-WIKI 하네스 보일러플레이트로 프로젝트 위키 초기화(repo 내장 모드). CLAUDE.md에 연동 규칙·검증 단계 병합.
- **SYMPO STUDIO 초기 구현**: claude.ai/design 프로토타입(콘솔·에디터·뷰어·리포트 + Microsite)을 Next.js 16 + TS로 구현. 빌드·브라우저 검증 통과. GitHub public repo 생성·푸시.
