# sympo-studio — 다음 과제

> **형식 계약 (훅이 파싱한다)**: 열린 과제 제목은 `### FE-N. 제목` / `### BE-N. 제목`.
> 코드 repo의 SessionStart 훅이 `### FE-`·`### BE-`로 시작하는 줄만 추출해 세션에 주입한다. 섹션 헤더는 주입되지 않으므로 **제목의 `FE-`/`BE-` 접두사가 유일한 구분자**다.
> 계약은 접두사뿐이라 아래 섹션 제목은 자유롭게 바꿔도 되고, 추출이 0건이면 훅이 형식 오류 문구를 대신 주입한다.
> 새 과제는 `무엇 → 왜 → 완료 기준`으로 추가하고, 종료되면 아래 종료 기록 표로 옮긴 뒤 지운다. 남은 번호를 다시 매기지 않는다.
> 분리 규칙의 근거는 [[0003-next-tasks-fe-be-split]], 작성 규칙 본문은 `CLAUDE.md`의 「Next-Tasks 작성 규칙」.

**FE는 서로 독립**이라 순서 없이 아무거나 착수할 수 있다(단, 대부분 대응하는 BE에 의존한다 — 각 과제의 `무엇`에 명시).
**BE는 번호가 곧 의존 순서**다. 근거는 [[0001-backend-for-working-demo]], [[0002-cloudflare-free-tier-stack]].

지금 당장 서버 없이 착수 가능한 것: **FE-9**, FE-4의 수료증 부분, FE-6의 뷰어 부분.

문제 정의의 정본은 [[field-experience]]다. 과제의 `왜`를 쓸 때 **거기 없는 내용을 근거로 삼지 않는다** — 기존 README가 그렇게 무너졌다([[0004-problem-redefinition]]).

## 열린 과제 — 프론트엔드(FE)

### FE-3. Q&A 화면 연결
**무엇** — 참가자 화면의 "질문 남기기"를 BE-3 API에 연결. 목록 갱신은 3~5초 폴링. 전송 중·실패·rate limit 초과 상태를 화면에 드러낸다. 입력 영역은 **고령 사용자 기준**으로 잡는다(글자 크기, 터치 타깃 44px 이상, 안내 문구를 화면에 상시 노출). **BE-3 의존.**
**왜** — 방문자가 남긴 것이 실제로 목록에 뜨는 순간이 이 데모의 하이라이트다. 실무에서 Q&A는 **반복 노출로 익숙해지며 사용률이 올랐으므로**([[field-experience]]), 처음 보는 사람도 설명 없이 쓸 수 있는지가 관건이다.
**완료 기준** — 다른 브라우저에서 남긴 질문이 폴링으로 나타남. rate limit 초과 시 사용자가 이유를 알 수 있음(조용히 실패하지 않음). 확대 없이 읽고 누를 수 있음.

### FE-4. 설문 흐름 재설계 + 수료증 클라이언트 생성
**무엇** — **현재의 2단 설문 구조부터 재검토한다.** 실무에서 이탈 지점을 없앴는데도 설문 응답률이 오르지 않았고 원인은 고령 사용자였다([[field-experience]]) — 끝나고 몰아 받는 2단 폼이 그 자체로 장벽일 수 있다. **세션이 끝날 때마다 1문항씩 즉시** 받는 안을 우선 검토하고, 정한 구조로 BE-4에 연결한다. 응답 완료 시 수료증을 `pdf-lib`로 **브라우저에서 생성**해 다운로드시킨다. **폼은 BE-4 의존, 수료증 생성은 독립.**
**왜** — **이 프로젝트가 도전하는 지점이 정확히 여기다**([[0004-problem-redefinition]]). 플랫폼 교체로는 실패했고 UI 접근은 시도된 적이 없다. "현장에서 사람이 하던 안내를 UI가 대신할 수 있는가"에 답하는 과제다. 서버 PDF 생성은 무료 티어에서 막히므로 수료증은 클라이언트 생성이 전제다([[0002-cloudflare-free-tier-stack]]).
**완료 기준** — 구조 선택의 근거를 ADR 또는 [[field-experience]] 갱신으로 남김(직관만으로 정하지 말 것). 응답이 저장되고 수료증 PDF가 브라우저에서 생성·다운로드됨. **응답률 가설은 검증되지 않았음을 README에 계속 명시.** 이번에 하지 않는 것: 이메일 발송 — 다운로드로 대체.

### FE-5. 리포트를 실측 데이터로 교체
**무엇** — `ReportScreen`의 하드코딩 샘플(`METRICS`/`OPS`)을 BE-5의 집계 응답으로 교체한다. **BE-5 의존.**
**왜** — 도구가 스스로를 측정하지 않으면 개선됐는지 알 수 없다는 것이 리포트 층을 넣은 이유다.
**완료 기준** — 실제 방문 데이터로 열람률이 그려짐. 실측으로 바뀐 지표에서 `샘플 데이터` 배지 제거, 남은 가상 수치는 배지 유지(README 고지와 UI 배지가 어긋나지 않을 것).

### FE-6. PDF.js 뷰어 + 현장 업로드 + 가상 강의자료 제작
**무엇** — PDF 렌더링을 **PDF.js 클라이언트**로 구현하고 페이지 이동·확대 UI를 만든다. 함께 **현장 업로드 경로**를 넣는다 — 자료 도착 전의 "준비 중" 상태 표시, 태블릿에서 올릴 수 있는 업로드 UI, 올린 즉시 참가자 화면 반영. 데모용 가상 강의자료 PDF 제작 포함. **업로드는 BE-6 의존**이나, 로컬 PDF로 뷰어 개발까지는 먼저 할 수 있다.
**왜** — 실무에서 **연자가 늦게 도착해 자료가 행사 진행 중에 올라왔다**([[field-experience]]). 자료를 올리는 곳은 사무실 책상이 아니라 현장이었고, 그때까지 빈 목록이 그냥 결함처럼 보였다. 뷰어만으로는 이 상황을 다루지 못한다.
**완료 기준** — **모바일·태블릿에서 페이지 이동·확대가 동작**(`<iframe>` 방식은 iOS Safari에서 깨져 기각). 자료 없는 상태가 "준비 중"으로 읽힘. 태블릿 화면에서 업로드 완주 가능. 번들은 lazy load로 초기 로딩에 포함되지 않음.

### FE-9. 네트워크 단절 대응
**무엇** — 폴링·서버 요청 실패를 화면에서 다루고, 마지막으로 받은 아젠다·자료 목록을 캐시해 오프라인에서도 표시한다. 참여 기능은 오프라인일 때 **명시적으로 비활성**하고 이유를 알린다(조용히 실패하지 않기). **BE 의존 없음** — 클라이언트 캐싱과 실패 처리다.
**왜** — 현장에서 **인터넷이 끊기는 일이 실제로 있었고**([[field-experience]]), 참가자 수십·수백 명이 같은 행사장 와이파이를 쓴다. **네트워크가 가장 느려지는 시점이 참여 기능을 써야 하는 시점**이다. [[0002-cloudflare-free-tier-stack]]이 3~5초 폴링을 전제하므로 이 대응 없이는 현장에서 화면이 비는 것으로 끝난다.
**완료 기준** — 네트워크를 끊은 상태에서 아젠다·자료가 계속 보임. 참여 기능이 비활성 상태와 사유를 노출. 복구 시 자동 재개.

### FE-10. 스튜디오 셸 브랜드 아이덴티티 적용
**무엇** — 스튜디오 셸(콘솔·에디터·리포트 크롬)에 브랜드 컬러를 입힌다. OKLCH 205도 틸(기존 프리셋 5종의 hue와 겹치지 않는 값)을 `lib/ui.ts`에 `brand`/`brandPress`/`brandSoft` 토큰으로 추가하고, 로고 마크(`components/Logo.tsx`)를 만들어 선택 상태·프라이머리 액션에만 제한적으로 적용한다. 참가자 마이크로사이트에는 이 색이 섞이지 않는다(테넌트 색과 도구 색 분리). **BE 의존 없음.**
**왜** — `lib/ui.ts`가 전부 그레이스케일이라 스튜디오 자체의 정체성이 없었다. 테넌트(행사) 쪽은 이미 프리셋 5종 + `derive()` 파생 시스템이 성숙한데, 정작 도구 자신은 색이 없는 비대칭 상태였다.
**완료 기준** — 셸·콘솔·에디터·리포트 반영 완료(lint·build·헤드리스 스크린샷으로 확인). `huni2/sympo-studio` fork에서 [PR #5](https://github.com/fomula91/sympo-studio/pull/5)로 올라감([[log]] 2026-08-17).

### FE-11. 다크 인트로/소개 페이지
**무엇** — `/intro` 라우트에 다크 톤 소개 페이지를 만든다(헤더·히어로·문제 정의·설계 판단 표·화면 4개·기술 선택·알고 있는 한계·CTA·푸터). 문구는 `docs/problem-redefinition`(PR #2, 미병합)의 정정된 README — field-experience.md로 검증된 두 층 문제 정의를 근거로 쓴다. **FE-10 의존**(브랜드 토큰·로고), BE 의존 없음.
**왜** — README가 이미 갖고 있던 서사를 방문자가 링크를 열고 30초 안에 훑을 수 있는 형태로 옮긴다. 루트가 아직 단일 페이지(FE-2 라우트 분리 전)라 신규 라우트로 뒀다.
**완료 기준** — `/intro`에서 lint·build 통과, 콘솔 에러 0건. 콘솔·에디터·뷰어·리포트 스크린샷 4장(`public/screens/`)이 실제 화면 반영. `design/intro-page` 브랜치(`design/brand-identity` 기반)에서 작업. [PR #6](https://github.com/fomula91/sympo-studio/pull/6)([[log]] 2026-08-17). 이후 라이트 기본 + 다크 토글로 전환됨([[log]] 2026-08-18) — 구조는 유지, 반응형·시각 리듬은 FE-12로 계승.

### FE-12. 소개 페이지 반응형 + 랜딩 재설계
**무엇** — `/intro`를 모바일·태블릿·데스크톱 완전 반응형으로 만들고, README 섹션을 그대로 옮긴 지금의 "문서형" 구성을 랜딩 페이지다운 시각적 리듬으로 다시 짠다. 기준은 [[design]] 6·7절(반응형 구간표, 재설계 원칙). **FE-11 의존**(구조·콘텐츠 재사용), BE 의존 없음.
**왜** — 사용자 피드백: 지금은 1440px 고정이라 폰에서 깨지고, "그냥 여기 프로젝트 PPT 같다"— 표·리스트가 이어지고 스케일 대비·여백 설계가 없다. 채용 담당자가 어떤 기기로 열지 모르는 링크이므로 반응형은 선택이 아니다.
**완료 기준** — 모바일(`<640px`)·태블릿(`640~1023px`)·데스크톱에서 레이아웃 확인(스크린샷 3종). 섹션 최소 하나 이상이 표/리스트가 아닌 다른 시각 레이아웃으로 교체됨. lint·build 통과.
**결과** — grid는 `auto-fit`+`minmax(min(Npx,100%), 1fr)` 패턴으로 미디어쿼리 없이 처리(고정 `minmax(480px,1fr)` 등은 좁은 화면에서 **바닥값이 컨테이너보다 커서 가로 오버플로를 일으킨다는 것**을 375px 실측에서 발견 — `min(Npx,100%)`로 바닥을 캡). 헤더 앵커 내비·"알고 있는 한계" 라벨 줄바꿈처럼 grid로 못 푸는 두 곳만 `globals.css`에 미디어쿼리(`.intro-anchors`, `.intro-limit-row`) 추가. "설계 판단" 7행 표를 Before→After 비교 카드로 교체(표 소거 + 자동 반응형 동시 해결), 결론 인용구를 브랜드 소프트 배경의 큰 텍스트로 격상해 섹션당 "큰 순간" 하나를 만듦. 375/820/1440px 3종 헤드리스 스크린샷, 가로 오버플로 0(재발 방지로 나머지 grid도 방어적으로 `min()` 처리). lint·build 통과.

## 열린 과제 — 백엔드(BE)

### BE-2. Cloudflare 배포 — 살아있는 링크 확보
**무엇** — `@opennextjs/cloudflare` 어댑터로 Workers/Pages 배포, D1·R2 바인딩, GitHub 연동 자동 배포.
**왜** — **링크 없는 완벽한 계획보다 링크 있는 절반이 낫다.** 포트폴리오에서 가장 먼저 읽히는 요소가 살아있는 URL이고, 이후 기능은 여기에 증분 배포하면 된다.
**완료 기준** — 공개 URL에서 앱이 뜨고 D1·R2 바인딩이 동작. push → 자동 배포. **결제 수단 미등록 상태 확인**(카드가 있으면 봇 트래픽에 청구서가 날아온다). 딥링크 확인은 FE-2 소관.

### BE-3. Q&A 저장·조회 API + 남용 통제
**무엇** — 질문 저장·조회 엔드포인트. **IP당 rate limit과 글자 수 제한을 이 과제에서 함께 넣는다**(나중이 아니라). 매일 시드 리셋 Cron 등록.
**왜** — 가장 쉽고 임팩트가 가장 크다. 여기까지만 해도 "실제 작동하는 데모"가 된다. rate limit을 미루면 봇이 폴링 엔드포인트를 때려 무료 티어 한도 2%가 순식간에 100%가 된다.
**완료 기준** — 저장·조회 왕복. rate limit 동작 확인. 시드 리셋 Cron 등록. 이번에 하지 않는 것: 좌장 승인 채팅(즉시성이 필요해 폴링으로 부족).

### BE-4. 설문 응답 수집 + 집계
**무엇** — 2단 설문 응답 저장과 `GROUP BY` 집계 엔드포인트.
**왜** — BE-3과 구조가 거의 같아 코드가 재사용된다. 설문 응답률은 리포트의 핵심 지표라 BE-5의 입력이기도 하다.
**완료 기준** — 응답이 저장되고 집계 수치가 조회됨. 남용 통제는 BE-3과 동일 정책 적용.

### BE-5. 이벤트 로그 적재 + OPS 집계
**무엇** — 세션별 열람·자료 열람·설문 완료를 이벤트 로그로 적재하고 집계 엔드포인트를 제공한다.
**왜** — BE-3·4에서 데이터가 쌓여야 의미가 생기므로 순서상 여기다.
**완료 기준** — 실제 방문 데이터가 적재되고 집계로 조회됨. **이벤트 로그 30일 보존 후 삭제 Cron 등록**(방치하면 무한 증가한다).

### BE-6. R2 자료 저장 + 업로드 + 서명 URL
**무엇** — 강의자료를 R2에 두고 서명 URL로 내준다. **업로드 엔드포인트도 여기 포함** — 행사 진행 중 현장에서 올리는 경로다(FE-6). 서버는 URL 발급과 저장만 하고 렌더링·변환에는 관여하지 않는다.
**왜** — 서버 PDF 파싱은 무료 티어에서 막히므로 "저장·조회는 서버, 렌더링은 클라이언트" 원칙을 그대로 적용한다([[0002-cloudflare-free-tier-stack]]). R2는 이그레스가 무료라 전송량 계산이 불필요하다.
**완료 기준** — 서명 URL로 PDF를 받을 수 있고 만료가 동작. **서버 CPU 사용 0**(파싱·변환 없음).

## 종료 기록

| # | 과제 | 결과 | 정본·근거 |
|---|---|---|---|
| 1 | 새 리포지토리로 이전 — 히스토리에 남은 원본 데이터 제거 | 완료. 기존 public 리포 삭제 후 동일 이름으로 재생성, 단일 초기 커밋(`cd1039c`)으로 시작 | 원격 clone 후 `git log -S` 조회 0건 (2026-08-15 [[log]]) |
| 2 | README 재작성 — 포트폴리오의 본체 | 완료. 문제 정의·설계 판단·알고 있는 한계 구성, 리포트 샘플 표기를 README와 UI 배지 양쪽에 명시 | `README.md`, `ReportScreen.tsx` (2026-08-15 [[log]]) |
| 3 | 정적 export + 무료 영구 배포 | **폐기.** 정적 전용으로는 Q&A·설문이라는 제품 핵심을 증명할 수 없어 백엔드를 붙이는 방향으로 대체. 배포 자체는 BE-2로 계승 | [[0001-backend-for-working-demo]] |
| BE-1 | D1 스키마 설계 + 이벤트 CRUD | 완료. 7개 테이블 + 이벤트 CRUD 4개 엔드포인트. 로컬 D1으로 생성·조회·수정·삭제 왕복, slug 충돌 회피, CASCADE 삭제, 설문 중복 방지 UNIQUE 확인. **서로 다른 이벤트가 각자의 아젠다·테마를 반환**하는 것까지 실측 | [[0005-d1-schema]], `migrations/0001_init.sql` (2026-08-15 [[log]]) |
| FE-1 | 이벤트별 편집 상태 분리 | 완료. `StudioState`에 평평했던 편집 필드(title/venue/date/host/cap/engage/presetId/mode/iconSet/density/keyVisual/kvPattern/sessions)를 `EventDetail`로 묶어 `EventItem`이 갖도록 이동, `editingId` + `patchEvent`로 "현재 편집 중인 이벤트만" 갱신. 콘솔 카드 클릭이 `editingId`만 바꾸고, "새 이벤트"가 실제로 `events` 배열에 새 항목을 만듦. 헤드리스 브라우저로 이벤트1 아젠다·테마·제목 편집 후 이벤트2가 영향받지 않는 것, 새 이벤트 생성이 독립 상태로 시작하는 것 실측. lint·build 통과, 콘솔 에러 0건 | `lib/types.ts`, `lib/data.ts`, `components/StudioApp.tsx`, `components/screens/{Console,Editor,Viewer,Report}Screen.tsx` (2026-08-24 [[log]]) |
| FE-2 | 라우트 분리 + 폰트 자체 호스팅 | 완료. 사용자와 상의해 범위를 좁힘 — `/[slug]` 공개 페이지는 만들지 않음(BE에 slug 조회 API 없음 + "행사 페이지는 관리자 화면 우측 미리보기로만" 결정). `/console`·`/events/[id]/edit`·`/report` 3개만 실제 라우트로 분리(`app/(studio)/` 라우트 그룹), 뷰어는 라우트 없이 `viewerOpen` 플래그로 현재 화면 위에 오버레이. 상태는 `StudioApp.tsx`(삭제)에서 `StudioProvider`(Context)로 끌어올려 레이아웃과 3개 페이지가 공유. Pretendard는 npm에서 가변 폰트 파일 1개(2.1MB)만 뽑아 `public/fonts/`에 커밋하고 `next/font/local`로 서빙 — 패키지 자체(72MB)는 `--no-save`로 설치해 `package.json`에 순증 없음. 헤드리스 브라우저로 3개 라우트 딥링크·카드클릭 네비게이션·뷰어 오버레이(URL 불변)·새 이벤트 생성 확인, 외부 요청 0건·콘솔 에러 0건 실측. lint·build 통과 | `lib/font.ts`, `components/Studio{Provider,Shell}.tsx`, `app/(studio)/**`, `app/{page,layout}.tsx` (2026-08-24 [[log]]) |
| FE-7 | 대비비 저장 게이트 실장 | 완료. `lib/theme.ts`의 근사식(`lum(L) = L³`, 채도·색상 무시)을 `culori`의 `wcagContrast`로 교체 — `contrastRows`가 이제 `theme.L.*`(명도 숫자) 대신 실제 `oklch(...)` 색 문자열을 비교한다. 신규 export `contrastAllPass(preset, mode)`로 게이트 판정을 한 곳에 모으고, `StudioShell.tsx`의 "공개하기" 버튼·내비 "뷰어" 아이콘 둘 다 이걸로 가드(미달 시 버튼 비활성 + "대비비 미달로 공개할 수 없음" 문구). 실측: 기존 내장 프리셋 5개는 근사·실측 양쪽 다 여유 있게 통과(가장 타이트해도 5.2:1, 기준 4.5:1) — 게이트를 걸어도 지금 데모는 안 깨짐. 이 저장소 첫 자동 테스트로 Vitest 도입(`npm run test`), `lib/theme.test.ts`에 5프리셋×라이트/다크 통과 회귀 + 인위적 고채도(`c=0.8`) 조합으로 실패 케이스(브랜드 위 텍스트 4.48:1, 기준 4.5:1 근소 미달) 검증 — 근사식이었다면 못 잡았을 미세 미달을 실제 sRGB 변환이라야 잡아낸다는 것의 증거. 헤드리스 브라우저로 UI 경로도 실측: 임시로 그 실패 프리셋을 `PRESETS`에 추가해 테마 탭에서 직접 선택 → 공개하기 비활성+사유 노출+강제 클릭해도 뷰어 안 열림, 정상 프리셋으로 되돌리면 재활성화까지 확인 후 원복(최종 diff엔 남지 않음). culori는 런타임 의존성(2.1kB, `@types/culori` devDependency 동반), lint·build·test 전부 통과, 콘솔 에러 0건 | `lib/theme.ts`, `lib/theme.test.ts`, `components/StudioShell.tsx`, `components/screens/EditorScreen.tsx` (2026-08-24 [[log]]) |
| FE-8 | 브랜드 컬러를 대표 이미지에서 추출 + 프리셋 축적 | 완료. `lib/colorExtract.ts` 신설 — 이미지를 48×48 캔버스로 축소해 픽셀을 채널당 8단계로 양자화한 히스토그램에 누적하고 무채색(채도 12% 미만) 버킷은 제외한 뒤 가장 큰 버킷을 `culori.oklch()`로 변환해 `{h, c}`를 뽑는다(로고·제품 이미지는 흰 배경이 대부분이라 단순 평균은 배경에 묻힘 — 무채색 제외가 핵심). `Preset` 타입을 `theme.ts`→`types.ts`로 옮겨 `StudioState.customPresets`(세션 동안 쌓이는 추출 프리셋, 이벤트가 아니라 앱 전체 공유)에 담고, `StudioProvider`가 `presets = [...PRESETS, ...customPresets]`를 컨텍스트로 노출해 4개 파일에 흩어져 있던 `PRESETS.find(...)` 반복을 단일 소스로 정리. `ThemeSection`의 "01 · 브랜드 프리셋" 그리드 끝에 "+ 이미지에서 추출" 타일 추가 — 추출 성공 시 스와치·라벨 입력·채도 슬라이더·실시간 대비비 배지가 뜨는 미리보기 패널이 열리고, `contrastAllPass`(FE-7)가 참일 때만 "프리셋으로 저장" 활성화(미달 시 "채도를 낮추면 통과할 수 있습니다" 안내 + 슬라이더로 채도를 낮춰 재시도). 저장하면 라벨을 슬러그화(중복 시 `-2` 접미사, BE-1의 slug 중복 처리와 같은 패턴)해 새 프리셋을 만들고 현재 이벤트에 즉시 적용. **실측으로 발견한 것** — 실제 sRGB에서 나올 수 있는 최고 채도급 색(마젠타 c≈0.32, 사이언·빨강·연두 등)을 전부 `derive()` 파이프라인에 넣어봐도 대비비 기준을 여유 있게 통과(최소 5.2:1) — `derive()`의 L-계층 차이가 넉넉해서, FE-7에서 만든 슬라이더/미달 UI는 실제 사진으로는 사실상 트리거되지 않는다(FE-7 테스트가 썼던 실패 케이스는 sRGB 밖의 인위적 채도였음). 그래도 게이트·수동 조정 경로 자체는 FE-7과 같은 검증된 함수를 그대로 재사용하므로 구조적으로는 정상 동작 — 완료 기준이 요구하는 "경로가 있음"은 충족. 헤드리스 브라우저로 실제 캔버스 이미지를 만들어 업로드 → 미리보기·대비비 배지 확인 → 저장 → 그리드에 새 타일 반영 → **SPA 네비게이션으로 다른 이벤트를 열어도 프리셋이 남아있고 라이브 프리뷰에 즉시 반영**되는 것까지 실측. lint·build·test 전부 통과, 콘솔 에러 0건 | `lib/colorExtract.ts`, `lib/types.ts`, `lib/theme.ts`, `components/StudioProvider.tsx`, `components/screens/{Editor,Viewer}Screen.tsx`, `components/StudioShell.tsx`, `app/(studio)/events/[id]/edit/page.tsx` (2026-08-24 [[log]]) |
